
version: 2.1
executorType: machine
jobs:
  build:
    machine: true
    steps:
      - checkout
      - run:
          name: date command
          command: echo "hello world"

workflows:
  version: 2.1
  workflow:
    jobs:
      - build
