
version: 2.1
executorType: machine
jobs:
  build:
    machine: true
    steps:
      - checkout
      - run:
          name: date command
          command: echo "hello world"
          name: comment on PR
          command: curl -X POST -u $GITHUB_USER_TOKEN:x-oauth-basic -d '{"body": "Hello"}' https://api.github.com/repos/G33tha/sunbird-devops/issues/$CIRCLE_PR_NUMBER/comments

workflows:
  version: 2.1
  workflow:
    jobs:
      - build
