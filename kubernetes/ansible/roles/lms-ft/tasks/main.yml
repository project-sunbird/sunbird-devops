---
- name: copy the template file
  template:
    src: "{{item}}.j2"
    dest: "/tmp/lms-ft.env"
    mode: 0755
  with_items:
    - lms-ft.env

- name: inject the lms-ft into apimanager pod
  shell: cd /tmp && source lms-ft.env && mvn -X clean verify