env:
  kafka:
    host: "{{ groups['kafka'][0] }}:9092"
    topic: "{{ env_name }}.prom.monitoring.metrics"
assessment_aggregator_threshold: "{{ assessment_aggregator_threshold | d(100) }}"
deduplication_threshold: "{{ deduplication_threshold | d(100) }}"
denormalization_threshold: "{{ denormalization_threshold | d(100) }}"
derived_deduplication_threshold: "{{ derived_deduplication_threshold | d(100) }}"
device_profileupdater_threshold: "{{ device_profileupdater_threshold | d(100) }}"
druidevents_validator_threshold: "{{ druidevents_validator_threshold | d(100) }}"
events_router_threshold: "{{ events_router_threshold | d(100) }}"
telemetry_extractor_threshold: "{{ telemetry_extractor_threshold | d(100) }}"
telemetry_location_updater_threshold: "{{ telemetry_location_updater_threshold | d(100) }}"
telemetry_redacter_threshold: "{{ telemetry_redacter_threshold | d(100) }}"
telemetry_router_threshold: "{{ telemetry_router_threshold | d(100) }}"
telemetry_validator_threshold: "{{ telemetry_validator_threshold | d(100) }}"
user_cacheupdater_threshold: "{{ user_cacheupdater_threshold | d(100) }}"
