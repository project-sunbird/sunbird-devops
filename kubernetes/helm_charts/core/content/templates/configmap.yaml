apiVersion: v1
data:
  {{- range $key, $val := .Values.contentenv }}
  {{ $key }}: {{ $val }}
  {{- end }}
  graph.ids: {{ ["domain"] | toJson }}
kind: ConfigMap
metadata:
  creationTimestamp: null
  name: {{ .Chart.Name }}-config
  namespace: {{ .Values.namespace }}

