apiVersion: v1
data:
  {{- range $key, $val := .Values.encenv }}
  {{ $key }}: {{default "''" $val }}
  {{- end }}
kind: ConfigMap
metadata:
  creationTimestamp: null
  name: {{ .Chart.Name }}-config
  namespace: {{ .Values.namespace }}
