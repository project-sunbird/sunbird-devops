pipeline {
  agent {
      label 'general-performance'
  }
  triggers {
        cron('H 0 * * *')
  }
  stages {
    stage('Pre-Build'){
      steps {
        sh('./pipelines/metrics-exporter/installDeps.sh')
      }
    }
    stage('Deploy') {
      steps {
        sh 'ENV=ntp-performance ./pipelines/metrics-exporter/deploy.sh'
      }
    }
  }
}
