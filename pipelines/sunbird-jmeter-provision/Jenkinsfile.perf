#!groovy

node('general-perf') {

    currentBuild.result = "SUCCESS"

    try {

       stage('Checkout'){

          checkout scm
          sh('./pipelines/setupDevopsCode.sh')
       }

       stage('Pre-Build'){

         sh('./pipelines/sunbird-jmeter-provision/installDeps.sh')

       }

       stage('Deploy'){
          sh 'ENV=perf ./pipelines/sunbird-jmeter-provision/deploy.sh'
       }

    }
    catch (err) {
        currentBuild.result = "FAILURE"
        throw err
    }
}
