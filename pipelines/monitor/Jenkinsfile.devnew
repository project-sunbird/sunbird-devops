#!groovy

node('general-devnew') {

    currentBuild.result = "SUCCESS"

    try {

       stage('Checkout'){

          checkout scm
          sh('./pipelines/setupDevopsCode.sh')
       }

       stage('Pre-Build'){

         sh('./pipelines/monitor/installDeps.sh')

       }

       stage('Deploy to Dev'){

         sh 'TARGET_ENV=devnew ./pipelines/monitor/deploy.sh'

       }
    }
    catch (err) {
        currentBuild.result = "FAILURE"
        throw err
    }

}
