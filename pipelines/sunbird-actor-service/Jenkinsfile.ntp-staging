#!groovy

node('docker-staging') {

    currentBuild.result = "SUCCESS"

    try {

       stage('Checkout'){

          checkout scm
       }

       stage('Pre-Build'){

         sh('./pipelines/sunbird-actor-service/installDeps.sh')

       }

      stage('Deploy to Dev'){
        sh 'METADATA_FILE=./pipelines/sunbird-actor-service/metadata.json ENV=ntp-staging ARTIFACT_LABEL=silver ./pipelines/sunbird-actor-service/deploy.sh'
        archive includes: "metadata.json"
       }
       }
    catch (err) {
        currentBuild.result = "FAILURE"
        throw err
    }

}
