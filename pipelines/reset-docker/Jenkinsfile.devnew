#!groovy

node('general-dev') {

    currentBuild.result = "SUCCESS"

    try {

       stage('Checkout'){

          checkout scm
          sh('./pipelines/setupDevopsCode.sh')
       }

       stage('Deploy'){
          sh('ansible-playbook -i ansible/inventories/dev sunbird-devops/ansible/reset_docker.yml --vault-password-file /run/secrets/vault-pass')
       }

    }
    catch (err) {
        currentBuild.result = "FAILURE"
        throw err
    }

}
