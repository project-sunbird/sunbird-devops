pipeline {
    agent {
        label 'general-staging'
    }
    stages {
        stage('Checkout') {
            steps {
                sh('./pipelines/setupDevopsCode.sh')
            }
        }

        stage('Pre-Build'){
            steps {
                sh('./sunbird-devops/ansible/installDeps.sh')
            }
        }

        stage('Deploy') {
            steps {
                sh '''
                cd sunbird-devops/ansible
                cat provision.yml
                ansible-playbook -i ../../ansible/inventories/ntp-staging provision.yml --tags es --vault-password-file /run/secrets/vault-pass
                '''
            }
        }
    }
}
