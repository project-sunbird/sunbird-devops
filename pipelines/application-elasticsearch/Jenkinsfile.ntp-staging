pipeline {
    agent {
        label 'general-staging'
    }
    stages {
        stage('Checkout') {
            steps {
                sh('./pipelines/setupDevopsCode.sh')
            }
        }

        stage('Pre-Build'){
            steps {
                sh('./ansible/installDeps.sh')
            }
        }

        stage('Deploy') {
            steps {
                sh '''
                cd ansible
                cat provision.yml
                ansible-playbook -i inventory/ntp-staging provision.yml --tags es --vault-password-file /run/secrets/vault-pass
                '''
            }
        }
    }
}
