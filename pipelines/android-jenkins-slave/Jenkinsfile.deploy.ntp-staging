pipeline {
    agent {
        label 'general-staging'
    }
    stages {
        stage('Deploy') {
            steps {
               sh 'METADATA_FILE=./pipelines/android-jenkins-slave/metadata.json ENV=dev ./pipelines/android-jenkins-slave/deploy.sh'

               archive includes: "metadata.json"
            }
        }
    }
}

