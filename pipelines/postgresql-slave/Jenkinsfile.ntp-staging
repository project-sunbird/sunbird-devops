pipeline {
    agent {
        label 'general-staging'
    }
    stages {
        stage('Deploy') {
            steps {
                sh '''
                cd ansible
                ansible-playbook -i inventory/ntp-staging provision.yml --tags postgresql-slave --vault-password-file /run/secrets/vault-pass
                '''
            }
        }
    }
}