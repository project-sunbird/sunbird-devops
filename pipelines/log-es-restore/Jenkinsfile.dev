pipeline {
  agent {
      label 'general-dev'
  }
  stages {
    stage('Checkout') {
      steps {
        sh('./pipelines/setupDevopsCodeOldStructure.sh')
      }
    }
    stage('Pre-Build'){
      steps {
        sh('./pipelines/log-es-restore/installDeps.sh')
      }
    }
    stage('Deploy') {
      steps {
        sh 'ENV=dev ./pipelines/log-es-restore/deploy.sh'
      }
    }
  }
}
