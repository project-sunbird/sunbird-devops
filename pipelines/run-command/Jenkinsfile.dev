#!groovy

node('general-dev') {

    currentBuild.result = "SUCCESS"

    try {

       stage('Checkout'){

          checkout scm
          sh('./pipelines/setupDevopsCode.sh')
       }
      ansiColor('xterm') {
       stage('Pre-Build'){

         sh('./pipelines/sunbird-player/installDeps.sh')
         sh 'METADATA_FILE=metadata.json  ENV=dev ./pipelines/run-command/deploy.sh'
         }

       }


    }
    catch (err) {
        currentBuild.result = "FAILURE"
        throw err
    }

}
