pipeline {
  agent {
      label 'general-dev'
  }
  triggers {
        cron('30 0 * * *')
  }
  stages {
    stage('Pre-Build'){
      steps {
        sh('./pipelines/es-backup/installDeps.sh')
      }
    }
    stage('Deploy') {
      steps {
        sh 'ENV=dev ./pipelines/es-backup/deploy.sh'
      }
    }
  }
}
