pipeline {
  agent {
      label 'general-staging'
  }
  stages {
    stage('Pre-Build'){
      steps {
        sh('./pipelines/es-backup/installDeps.sh')
      }
    }
    stage('Deploy') {
      steps {
        sh 'ENV=ntp-staging ./pipelines/es-backup/deploy.sh'
      }
    }
  }
}
