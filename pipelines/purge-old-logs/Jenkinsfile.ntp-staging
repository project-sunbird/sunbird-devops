pipeline {
  agent {
      label 'general-staging'
  }
  triggers {
        cron('H 0 * * *')
  }
  stages {
    stage('Pre-Build'){
      steps {
        sh('./pipelines/purge-old-logs/installDeps.sh')
      }
    }
    stage('Deploy') {
      steps {
        sh 'ENV=ntp-staging ./pipelines/purge-old-logs/deploy.sh'
      }
    }
  }
}
