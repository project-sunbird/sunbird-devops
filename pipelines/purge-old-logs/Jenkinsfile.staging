pipeline {
  agent {
      label 'general-staging'
  }
  triggers {
        cron('@midnight')
  }
  stages {
    stage('Pre-Build'){
      steps {
        sh('./pipelines/purge-old-logs/installDeps.sh')
      }
    }
    stage('Deploy') {
      steps {
        sh 'ENV=staging ./pipelines/purge-old-logs/deploy.sh'
      }
    }
  }
}
