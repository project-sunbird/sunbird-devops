#!groovy

node('build-slave') {

    currentBuild.result = "SUCCESS"

    try {
       stage('Checkout'){
          checkout scm
          sh('./pipelines/setupDevopsCode.sh')
          sh('rm -rf sunbird-auth') 
          sh('git clone https://github.com/project-sunbird/sunbird-auth.git')
       }

       stage('Build'){
         sh('pwd')
         sh('ls')
         sh('cd sunbird-devops && ls')

         sh('cd sunbird-auth/keycloak/sms-provider && mvn package')

         sh('pwd')
         sh('ls')
         archiveArtifacts artifacts: '/home/ops/workspace/New_Build/Keycloak_Build/sunbird-auth/keycloak/sms-provider/target/keycloak-email-phone-autthenticator-1.0-SNAPSHOT-shaded.jar'
         
         sh('ls')
       }
    }
    catch (err) {
        currentBuild.result = "FAILURE"
        throw err
    }
}
