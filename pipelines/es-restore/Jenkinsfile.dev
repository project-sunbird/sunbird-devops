pipeline {
  agent {
      label 'general-dev'
  }
  stages {
    stage('Pre-Build'){
      steps {
        sh('./pipelines/es-restore/installDeps.sh')
      }
    }
    stage('Deploy') {
      steps {
        sh 'ENV=dev ./pipelines/es-restore/deploy.sh'
      }
    }
  }
}
