#!groovy

node('general-staging') {

    currentBuild.result = "SUCCESS"

    try {

       stage('Checkout'){

          checkout scm
          sh('./pipelines/setupDevopsCodeOldStructure.sh')
       }

       stage('Pre-Build'){

         sh('./pipelines/sunbird-content-service/installDeps.sh')

       }
      stage('Deploy to Dev'){
        sh 'METADATA_FILE=./pipelines/sunbird-content-service/metadata.json ENV=ntp-staging ARTIFACT_LABEL=bronze ./pipelines/sunbird-content-service/deployOldStructure.sh'
        archive includes: "metadata.json"
       }
    }
    catch (err) {
        currentBuild.result = "FAILURE"
        throw err
    }

}
