#!groovy

node('general-staging') {

    currentBuild.result = "SUCCESS"

    try {

       stage('Checkout'){

          checkout scm
       }

       stage('Pre-Build'){

         sh('./pipelines/sunbird-content-service/installDeps.sh')

       }
      stage('Deploy to Dev'){
        sh 'METADATA_FILE=./pipelines/sunbird-content-service/metadata.json ENV=ntp-staging ARTIFACT_LABEL=silver ./pipelines/sunbird-content-service/deploy.sh'
        archive includes: "metadata.json"
       }

    }
    catch (err) {
        currentBuild.result = "FAILURE"
        throw err
    }

}
