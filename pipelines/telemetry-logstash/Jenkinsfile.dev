#!groovy

node('general-dev') {

    currentBuild.result = "SUCCESS"

    try {

       stage('Checkout'){

          checkout scm
          sh('./pipelines/setupDevopsCode.sh')
       }

       stage('Pre-Build'){

         sh('./pipelines/telemetry-logstash/installDeps.sh')

       }

       stage('Deploy'){
          sh ('./pipelines/telemetry-logstash/deploy.sh dev')
       }
    }
    catch (err) {
        currentBuild.result = "FAILURE"
        throw err
    }

}
