pipeline {
    agent {
        label 'general-spike'
    }
    stages {
        stage('Deploy') {
            steps {
                sh '''
                cd ansible
                ansible-playbook -i inventory/spike provision.yml --tags postgresql-master --vault-password-file /run/secrets/vault-pass
                '''
            }
        }
    }
}