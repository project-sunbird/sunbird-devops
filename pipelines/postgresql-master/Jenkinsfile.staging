pipeline {
    agent {
        label 'general-staging'
    }
    stages {
        stage('Pre-Build'){
            steps {
                sh('./ansible/installDeps.sh')
            }
        }
        stage('Deploy') {
            steps {
                sh '''
                cd ansible
                ansible-playbook -i inventory/staging provision.yml --tags postgresql-master --vault-password-file /run/secrets/vault-pass
                '''
            }
        }
    }
}