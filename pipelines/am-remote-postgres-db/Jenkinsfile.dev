pipeline {
    agent {
        label 'kong-remote-postgres-db'
    }
    stages {
        stage('Deploy') {
            steps {
                sh '''
                cd ansible
                ansible-playbook -i inventory/dev api-manager.yml --tags kong-remote-postgres-db --vault-password-file /run/secrets/vault-pass
                '''
            }
        }
    }
}