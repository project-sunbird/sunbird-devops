pipeline {
  agent {
      label 'general-performance'
  }
  stages {
    stage('Pre-Build'){
      steps {
        sh('./ansible/installDeps.sh')
      }
    }
    stage('Deploy') {
      steps {
        step ([$class: 'CopyArtifact',
          projectName: 'Staging/Proxy',
          filter: 'metadata.json']);
        sh 'cat metadata.json'
        sh 'METADATA_FILE=metadata.json ARTIFACT_LABEL=gold ENV=ntp-performance ./pipelines/proxy/deploy.sh'
        archive includes: "metadata.json"
      }
    }
  }
}
