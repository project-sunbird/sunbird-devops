pipeline {
    agent {
        label 'general-spike'
    }
    stages {
        stage('Deploy') {
            steps {
                sh '''
                cd ansible
                ansible-playbook -i inventory/spike postgresql-slave-to-master-promotion.yml --vault-password-file /run/secrets/vault-pass
                '''
            }
        }
    }
}