pipeline {
    agent {
        label 'general-dev'
    }
    triggers {
        cron('@midnight')
    }
    stages {
        stage('Checkout') {
            steps {
                sh('./pipelines/setupDevopsCodeOldStructure.sh')
            }
        }
        stage('Pre-Build'){
            steps {
                sh('./ansible/installDeps.sh')
            }
        }
        stage('Deploy') {
            steps {
                sh '''
                cd ansible
                ansible-playbook -v -i inventory/dev swarm-agent-docker-prune.yml --vault-password-file /run/secrets/vault-pass
                '''
            }
        }
    }
}
