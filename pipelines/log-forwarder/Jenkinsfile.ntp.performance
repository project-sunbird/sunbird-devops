pipeline {
  agent {
      label 'general-performance'
  }
  triggers {
        cron('H 0 * * *')
  }
  stages {
    stage('Pre-Build'){
      steps {
        sh('./pipelines/log-forwarder/installDeps.sh')
      }
    }
    stage('Deploy') {
      steps {
        sh 'ENV=ntp-performance ./pipelines/log-forwarder/deploy.sh'
      }
    }
  }
}
