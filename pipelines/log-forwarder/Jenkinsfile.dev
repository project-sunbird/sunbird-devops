pipeline {
  agent {
      label 'general-dev'
  }
  triggers {
        cron('H 0 * * *')
  }
  stages {
    stage('Pre-Build'){
      steps {
        sh('./pipelines/log-forwarder/installDeps.sh')
      }
    }
    stage('Deploy') {
      steps {
        sh 'ENV=dev ./pipelines/log-forwarder/deploy.sh'
      }
    }
  }
}
