apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Chart.Name }}-config
  namespace: {{ .Release.Namespace }}
data:
{{ (.Files.Glob "files/kong-apis.json").AsConfig | indent 2 }}
{{ (.Files.Glob "files/*").AsConfig | indent 2 }}