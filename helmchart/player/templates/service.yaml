apiVersion: v1
kind: Service
metadata:
  name: player
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Chart.Name }}
spec:
  ports:
  - name: http-{{ .Chart.Name }}
    protocol: TCP
    port: {{ .Values.targetPort }}
  selector:
    app: {{ .Chart.Name }}
