postgresql_databases:
  - name: "{{kong_postgres_database}}"

# List of users to be created (optional)
postgresql_users:
  - name: "{{kong_postgres_user}}"
    pass: "{{kong_postgres_password}}"
    db: "{{kong_postgres_database}}"
    priv: "ALL"
