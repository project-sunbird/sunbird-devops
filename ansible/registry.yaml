---
- hosts: registry
  become: true
  vars_files:
    - ['{{inventory_dir}}/secrets.yml', 'secrets/{{env}}.yml']
  roles:
    - java8
  tasks:
    - name: Install Maven
      apt: name=maven state=present
