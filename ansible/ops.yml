- hosts: all
  become: yes
  vars_files:
    - 'secrets/{{env}}.yml'
  roles:
    - vm-agents-filebeat
  tags:
    - log-forwarder

- hosts: all
  roles:
    - vm-agents-nodeexporter
  tags:
    - exporter-node