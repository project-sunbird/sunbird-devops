- hosts: swarm-agent-for-prometheus
  become: yes
  vars_files:
    - ['{{inventory_dir}}/secrets.yml', 'secrets/{{env}}.yml']
  tasks:
    - name: Create prometheus data dir
      file:
        path: "{{ prometheus_mount_point}}" 
        state: directory
        mode: 0755
        owner: 'nobody'
        group: 'nogroup'
  tags:
    - prometheus

- hosts: swarm-agent-for-alertmanager
  become: yes
  vars_files:
    - ['{{inventory_dir}}/secrets.yml', 'secrets/{{env}}.yml']
  tasks:
    - name: Create alertmanager data dir
      file:
        path: /var/dockerdata/alertmanager/data
        state: directory
        mode: 0755
  tags:
    - alertmanager

- hosts: swarm-bootstrap-manager
  gather_facts: false
  become: yes
  vars_files:
    - ['{{inventory_dir}}/secrets.yml', 'secrets/{{env}}.yml']
  tasks:
    - name: Add label for prometheus node
      shell: "docker node update --label-add prometheus=1 {{ hostvars[groups['swarm-agent-for-prometheus'][0]]['ansible_hostname'] }}"
    - name: Remove old style label for alertmanager node
      shell: "docker node update --label-rm monitor {{ hostvars[groups['swarm-agent-for-alertmanager'][0]]['ansible_hostname'] }}"
      ignore_errors: true
    - name: Add label for alertmanager node
      shell: "docker node update --label-add alertmanager=1 {{ hostvars[groups['swarm-agent-for-alertmanager'][0]]['ansible_hostname'] }}"
  tags:
    - monitor-labels

- hosts: swarm-bootstrap-manager
  gather_facts: false
  become: yes
  vars_files:
    - ['{{inventory_dir}}/secrets.yml', 'secrets/{{env}}.yml']
  roles:
    - stack-monitor
    - {role: 'monit', monit_checks: ['alertmanager', 'prometheus']}
  tags:
    - stack-monitor

- hosts: swarm-dashboard
  gather_facts: false
  become: yes
  vars_files:
    - ['{{inventory_dir}}/secrets.yml', 'secrets/{{env}}.yml']
  roles:
    - stack-monitor-stateful
    - {role: 'monit', monit_checks: ['alertmanager', 'prometheus']}
  tags:
    - stack-monitor-stateful

- hosts: swarm-dashboard
  become: yes
  vars_files:
    - ['{{inventory_dir}}/secrets.yml', 'secrets/{{env}}.yml']
  roles:
    - stack-grafana
  tags:
    - stack-grafana

- hosts: all
  become: yes
  gather_facts: false
  vars_files:
    - ['{{inventory_dir}}/secrets.yml', 'secrets/{{env}}.yml']
  roles:
    - vm-agents-nodeexporter
  tags:
    - metrics-exporter

- hosts: process-exporter
  become: yes
  vars_files:
    - ['{{inventory_dir}}/secrets.yml', 'secrets/{{env}}.yml']
  roles:
    - vm-agents-processexporter
  tags:
    - metrics-exporter

- hosts: swarm-bootstrap-manager
  gather_facts: false
  become: yes
  vars_files:
    - ['{{inventory_dir}}/secrets.yml','secrets/{{env}}.yml']
  roles:
    - prometheus_config
  tags:
    - lp_dp_monitoring
