---
- name: Checking Ansible
  vars:
    hardware:
    version: true
    application: localhost
    host: casandra
  import_playbook: system_check.yml

- name: Checking disc
  vars:
    hardware: true
    version:
    application: localhost
    host: all
  import_playbook: system_check.yml

- name: Checking Elasticsearch
  vars:
    hardware: true
    version: true
    application: es
    host: localhost
  import_playbook: system_check.yml

- name: Checking Postgres
  vars:
    hardware: true
    version: true
    application: postgres
    host: localhost
  import_playbook: system_check.yml

- name: Checking Cassandra
  vars:
    hardware: true
    version: true
    application: cassandra
    host: localhost
  import_playbook: system_check.yml
