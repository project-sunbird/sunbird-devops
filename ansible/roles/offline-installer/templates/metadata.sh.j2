#!/bin/bash

version=$(cat src/package.json | jq -r ".version")

# constructing the latest.json file
echo "{\"version\":\"${version}\",\"windows\":{\"32bit\":\"diskha_${version}_32bit.exe\",\"64bit\":\"diskha_${version}_64bit.exe\"},\"linux\":{\"64bit\":\"diskah_${version}_64bit.deb\"}}" > latest.json
