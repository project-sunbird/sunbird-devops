#!/bin/bash

# Downgrade the node version
npm install -g n
n 8.16

#Build the offline installer
npm install
npm run dist
npm run dist-win64
npm run dist-linux

# Copy the build artifacts to specific folder
cp /private/src/dist/DIKSHA\ Setup\ 1.0.0.exe offline_artifacts/{{build_number}}/
cp /private/src/dist/DIKSHA_1.0.0_amd64.deb offline_artifacts/{{build_number}}/
