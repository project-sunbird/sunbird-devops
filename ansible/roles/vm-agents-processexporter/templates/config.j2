process_names:
{% if hostvars[inventory_hostname]['ansible_default_ipv4']['address'] in groups['kafka'] %}
  - name: "kafka"
    comm:
    - java
    cmdline:
    - .+kafka.+
{% endif %}
{% if hostvars[inventory_hostname]['ansible_default_ipv4']['address'] in groups['secor'] %}
  - name: "secor"
    comm:
    - java
    cmdline:
    - .+secor.+
{% endif %}
{% if hostvars[inventory_hostname]['ansible_default_ipv4']['address'] in groups['kafka'] %}
  - name: "zookeeper"
    comm:
    - java
    cmdline:
    - .+zookeeper.+
{% endif %}
{% if hostvars[inventory_hostname]['ansible_default_ipv4']['address'] in groups['yarn'] %}
  - name: "yarn"
    comm:
    - java
    cmdline:
    - .+yarn.+
{% endif %}
{% if hostvars[inventory_hostname]['ansible_default_ipv4']['address'] in groups['cassandra'] %}
  - name: "cassandra"
    comm:
    - java
    cmdline:
    - .+cassandra.+
{% endif %}
{% if hostvars[inventory_hostname]['ansible_default_ipv4']['address'] in groups['es'] %}
  - name: "elasticsearch"
    comm:
    - java
    cmdline:
    - .+elasticsearch.+
{% endif %}
{% if hostvars[inventory_hostname]['ansible_default_ipv4']['address'] in groups['logstash'] %}
  - name: "logstash"
    comm:
    - java
    cmdline:
    - .+logstash.+
{% endif %}
{% if hostvars[inventory_hostname]['ansible_default_ipv4']['address'] in groups['analytics'] %}
  - name: "analyticsapi"
    comm:
    - java
    cmdline:
    - .+analytic.+
{% endif %}
  - name: {{'"{{.Comm}}"'}}
    cmdline:
    - '.+'
