---
# Guideline for PR Merging: https://project-sunbird.atlassian.net/wiki/spaces/DevOps/pages/1210777612/API+Manager+Kong+BEST+Practices+Documentation.

# OnboardAPI's: https://project-sunbird.atlassian.net/wiki/spaces/DevOps/pages/1205207131/Best+Practces+For+Onboarding+API+s+In+Sunbird

# Onboard Consumers: https://project-sunbird.atlassian.net/wiki/spaces/DevOps/pages/1202847861/Best+Practces+For+Onboarding+Consumers+In+Sunbird
kong_admin_api_url: "http://{{kong_host}}:8001"

# Default statsd_metrics
statsd_host: statsd-service
statsd_port: 9125

statsd_pulgin:
  name: statsd
  config.host: "{{ statsd_host }}"
  config.port: "{{ statsd_port }}"

# Default Rate limits
small_rate_limit_per_hour: 100
medium_rate_limit_per_hour: 5000
x_medium_rate_limit_per_hour: 5000
large_rate_limit_per_hour: 10000
x2_large_rate_limit_per_hour: 15000
x_large_rate_limit_per_hour: 30000

premium_consumer_small_rate_limit_per_hour: 1000
premium_consumer_medium_rate_limit_per_hour: 10000
premium_consumer_large_rate_limit_per_hour: 100000

# Default max request size in MB
small_request_size_limit: 1
medium_request_size_limit: 10
large_request_size_limit: 100

# External URL's
freshDesk_url: "http://dummy.freshDesk.url"


#Managed Learn Prefixes
sl_assessments_prefix: /assessment
sl_kendra_prefix: /kendra
sl_unnati_prefix: /improvement-project
sl_sunbird_prefix: /sl-sunbird
sl_usermanagement_prefix: /user-management
sl_dhiti_prefix: /dhiti

# Service URLs
unnati_service_url: "http://unnati-service:4301"
kendra_service_url: "http://kendra-service:4202"
dhiti_service_url: "http://dhiti-service:4700"
sl_assessments_service_url: "http://sl-assessments-service:4201"
sl_sunbird_service_url: "http://sl-sunbird-service:4551"
sl_usermanagement_service_url: "http://sl-usermanagement-service:4101"
highcharts_service_url: "http://highcharts-service:8080"
gotenberg_service_url: "http://gotenberg-service:3000"

premium_consumer_rate_limits:
  - api: createContent
    config.hour: "{{ premium_consumer_small_rate_limit_per_hour }}"
    config.limit_by: credential
    config.policy: local
  - api: updateContent
    config.hour: "{{ premium_consumer_medium_rate_limit_per_hour }}"
    config.limit_by: credential
    config.policy: local
  - api: readContent
    config.hour: "{{ premium_consumer_large_rate_limit_per_hour }}"
    config.limit_by: credential
    config.policy: local

kong_apis:

  - name: observationsInstance
    request_path: "{{ sl_dhiti_prefix }}/api/v1/observations/instance"
    upstream_url: "{{ dhiti_service_url }}{{ sl_dhiti_prefix }}/api/v1/observations/instance"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: entityReportDhiti
    request_path: "{{ sl_dhiti_prefix }}/api/v1/observations/entity"
    upstream_url: "{{ dhiti_service_url }}{{ sl_dhiti_prefix }}/api/v1/observations/entity"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: observationReport
    request_path: "{{ sl_dhiti_prefix }}/api/v1/observations/report"
    upstream_url: "{{ dhiti_service_url }}{{ sl_dhiti_prefix }}/api/v1/observations/report"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: listProgramsDhiti
    request_path: "{{ sl_dhiti_prefix }}/api/v1/assessments/listPrograms"
    upstream_url: "{{ dhiti_service_url }}{{ sl_dhiti_prefix }}/api/v1/assessments/listPrograms"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: entityAssessmentsDhiti
    request_path: "{{ sl_dhiti_prefix }}/api/v1/assessments/entity"
    upstream_url: "{{ dhiti_service_url }}{{ sl_dhiti_prefix }}/api/v1/assessments/entity"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: assessmentsPdfReportsDhiti
    request_path: "{{ sl_dhiti_prefix }}/api/v1/assessments/pdfReports"
    upstream_url: "{{ dhiti_service_url }}{{ sl_dhiti_prefix }}/api/v1/assessments/pdfReports"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: observationsPdfReportsDhiti
    request_path: "{{ sl_dhiti_prefix }}/api/v1/observations/pdfReports"
    upstream_url: "{{ dhiti_service_url }}{{ sl_dhiti_prefix }}/api/v1/observations/pdfReports"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: courseEnrollmentDhiti
    request_path: "{{ sl_dhiti_prefix }}/api/v1/shikshalokam/courseEnrollment"
    upstream_url: "{{ dhiti_service_url }}{{ sl_dhiti_prefix }}/api/v1/shikshalokam/courseEnrollment"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: contentViewDhiti
    request_path: "{{ sl_dhiti_prefix }}/api/v1/shikshalokam/contentView"
    upstream_url: "{{ dhiti_service_url }}{{ sl_dhiti_prefix }}/api/v1/shikshalokam/contentView"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: usageByContentDhiti
    request_path: "{{ sl_dhiti_prefix }}/api/v1/shikshalokam/usageByContent"
    upstream_url: "{{ dhiti_service_url }}{{ sl_dhiti_prefix }}/api/v1/shikshalokam/usageByContent"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: listObservationNamesDhiti
    request_path: "{{ sl_dhiti_prefix }}/api/v1/observations/listObservationNames"
    upstream_url: "{{ dhiti_service_url }}{{ sl_dhiti_prefix }}/api/v1/observations/listObservationNames"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: entityObservationReportDhiti
    request_path: "{{ sl_dhiti_prefix }}/api/v1/observations/entityObservationReport"
    upstream_url: "{{ dhiti_service_url }}{{ sl_dhiti_prefix }}/api/v1/observations/entityObservationReport"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: instanceObservationScoreReportDhiti
    request_path: "{{ sl_dhiti_prefix }}/api/v1/observations/instanceObservationScoreReport"
    upstream_url: "{{ dhiti_service_url }}{{ sl_dhiti_prefix }}/api/v1/observations/instanceObservationScoreReport"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: entityScoreReportDhiti
    request_path: "{{ sl_dhiti_prefix }}/api/v2/observations/entityScoreReport"
    upstream_url: "{{ dhiti_service_url }}{{ sl_dhiti_prefix }}/api/v2/observations/entityScoreReport"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: scoreReportDhiti
    request_path: "{{ sl_dhiti_prefix }}/api/v1/observations/scoreReport"
    upstream_url: "{{ dhiti_service_url }}{{ sl_dhiti_prefix }}/api/v1/observations/scoreReport"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: observationSubmissionsCountDhiti
    request_path: "{{ sl_dhiti_prefix }}/api/v1/observations/observationSubmissionsCount"
    upstream_url: "{{ dhiti_service_url }}{{ sl_dhiti_prefix }}/api/v1/observations/observationSubmissionsCount"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: observationScorePdfReportDhiti
    request_path: "{{ sl_dhiti_prefix }}/api/v1/observations/observationScorePdfReport"
    upstream_url: "{{ dhiti_service_url }}{{ sl_dhiti_prefix }}/api/v1/observations/observationScorePdfReport"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: listObservationSolutionsDhiti
    request_path: "{{ sl_dhiti_prefix }}/api/v2/observations/listObservationSolutions"
    upstream_url: "{{ dhiti_service_url }}{{ sl_dhiti_prefix }}/api/v2/observations/listObservationSolutions"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: entitySolutionScoreReportDhiti
    request_path: "{{ sl_dhiti_prefix }}/api/v1/observations/entitySolutionScoreReport"
    upstream_url: "{{ dhiti_service_url }}{{ sl_dhiti_prefix }}/api/v1/observations/entitySolutionScoreReport"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: entitySolutionReportDhiti
    request_path: "{{ sl_dhiti_prefix }}/api/v1/observations/entitySolutionReport"
    upstream_url: "{{ dhiti_service_url }}{{ sl_dhiti_prefix }}/api/v1/observations/entitySolutionReport"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: entitySolutionReportDhitiV2
    request_path: "{{ sl_dhiti_prefix }}/api/v2/observations/entitySolutionReport"
    upstream_url: "{{ dhiti_service_url }}{{ sl_dhiti_prefix }}/api/v2/observations/entitySolutionReport"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: pdfReportDhiti
    request_path: "{{ sl_dhiti_prefix }}/api/v1/unnati/pdfReport"
    upstream_url: "{{ dhiti_service_url }}{{ sl_dhiti_prefix }}/api/v1/unnati/pdfReport"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: addTaskReportDhiti
    request_path: "{{ sl_dhiti_prefix }}/api/v1/unnati/addTaskReport"
    upstream_url: "{{ dhiti_service_url }}{{ sl_dhiti_prefix }}/api/v1/unnati/addTaskReport"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: monthlyReportDhiti
    request_path: "{{ sl_dhiti_prefix }}/api/v1/unnati/monthlyReport"
    upstream_url: "{{ dhiti_service_url }}{{ sl_dhiti_prefix }}/api/v1/unnati/monthlyReport"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: viewProjectReportDhiti
    request_path: "{{ sl_dhiti_prefix }}/api/v1/improvement-project/viewProjectReport"
    upstream_url: "{{ dhiti_service_url }}{{ sl_dhiti_prefix }}/api/v1/improvement-project/viewProjectReport"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: entityReportImprovementProjectDhiti
    request_path: "{{ sl_dhiti_prefix }}/api/v1/improvement-project/entityReport"
    upstream_url: "{{ dhiti_service_url }}{{ sl_dhiti_prefix }}/api/v1/improvement-project/entityReport"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: newsFeedReportDhiti
    request_path: "{{ sl_dhiti_prefix }}/api/v1/unnati/newsFeedReport"
    upstream_url: "{{ dhiti_service_url }}{{ sl_dhiti_prefix }}/api/v1/unnati/newsFeedReport"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: listAllEvidencesDhiti
    request_path: "{{ sl_dhiti_prefix }}/api/v1/observations/listAllEvidences"
    upstream_url: "{{ dhiti_service_url }}{{ sl_dhiti_prefix }}/api/v1/observations/listAllEvidences"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"
      
  - name: listAssessmentProgramsDhiti
    request_path: "{{ sl_dhiti_prefix }}/api/v1/assessments/listAssessmentPrograms"
    upstream_url: "{{ dhiti_service_url }}{{ sl_dhiti_prefix }}/api/v1/assessments/listAssessmentPrograms"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: listEntitiesDhiti
    request_path: "{{ sl_dhiti_prefix }}/api/v1/assessments/listEntities"
    upstream_url: "{{ dhiti_service_url }}{{ sl_dhiti_prefix }}/api/v1/assessments/listEntities"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: listImprovementProjectsDhiti
    request_path: "{{ sl_dhiti_prefix }}/api/v1/assessments/listImprovementProjects"
    upstream_url: "{{ dhiti_service_url }}{{ sl_dhiti_prefix }}/api/v1/assessments/listImprovementProjects"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: scorePdfReportsByCriteriaDhiti
    request_path: "{{ sl_dhiti_prefix }}/api/v1/observations/scorePdfReportsByCriteria"
    upstream_url: "{{ dhiti_service_url }}{{ sl_dhiti_prefix }}/api/v1/observations/scorePdfReportsByCriteria"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: submissionReportDhiti
    request_path: "{{ sl_dhiti_prefix }}/api/v1/surveys/submissionReport"
    upstream_url: "{{ dhiti_service_url }}{{ sl_dhiti_prefix }}/api/v1/surveys/submissionReport"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: solutionReportDhiti
    request_path: "{{ sl_dhiti_prefix }}/api/v1/surveys/solutionReport"
    upstream_url: "{{ dhiti_service_url }}{{ sl_dhiti_prefix }}/api/v1/surveys/solutionReport"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: instanceReportByCriteriaDhiti
    request_path: "{{ sl_dhiti_prefix }}/api/v1/observations/instanceReportByCriteria"
    upstream_url: "{{ dhiti_service_url }}{{ sl_dhiti_prefix }}/api/v1/observations/instanceReportByCriteria"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: instanceScoreReportByCriteriaDhiti
    request_path: "{{ sl_dhiti_prefix }}/api/v1/observations/instanceScoreReportByCriteria"
    upstream_url: "{{ dhiti_service_url }}{{ sl_dhiti_prefix }}/api/v1/observations/instanceScoreReportByCriteria"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: entityReportByCriteriaDhiti
    request_path: "{{ sl_dhiti_prefix }}/api/v1/observations/entityReportByCriteria"
    upstream_url: "{{ dhiti_service_url }}{{ sl_dhiti_prefix }}/api/v1/observations/entityReportByCriteria"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: entityScoreReportByCriteria
    request_path: "{{ sl_dhiti_prefix }}/api/v1/observations/entityScoreReportByCriteria"
    upstream_url: "{{ dhiti_service_url }}{{ sl_dhiti_prefix }}/api/v1/observations/entityScoreReportByCriteria"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: observationReportByCriteriaDhitit
    request_path: "{{ sl_dhiti_prefix }}/api/v1/observations/observationReportByCriteria"
    upstream_url: "{{ dhiti_service_url }}{{ sl_dhiti_prefix }}/api/v1/observations/observationReportByCriteria"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: observationScoreReportByCriteriaDhiti
    request_path: "{{ sl_dhiti_prefix }}/api/v1/observations/observationScoreReportByCriteria"
    upstream_url: "{{ dhiti_service_url }}{{ sl_dhiti_prefix }}/api/v1/observations/observationScoreReportByCriteria"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: programsListDhiti
    request_path: "{{ sl_dhiti_prefix }}/api/v1/programs/list"
    upstream_url: "{{ dhiti_service_url }}{{ sl_dhiti_prefix }}/api/v1/programs/list"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: solutionsListDhiti
    request_path: "{{ sl_dhiti_prefix }}/api/v1/solutions/list"
    upstream_url: "{{ dhiti_service_url }}{{ sl_dhiti_prefix }}/api/v1/solutions/list"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: assessmentEntityReportDhiti
    request_path: "{{ sl_dhiti_prefix }}/api/v1/assessments/entityReport"
    upstream_url: "{{ dhiti_service_url }}{{ sl_dhiti_prefix }}/api/v1/assessments/entityReport"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: getAllResponsesOfQuestionDhiti
    request_path: "{{ sl_dhiti_prefix }}/api/v1/surveys/getAllResponsesOfQuestion"
    upstream_url: "{{ dhiti_service_url }}{{ sl_dhiti_prefix }}/api/v1/surveys/getAllResponsesOfQuestion"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"


  - name: bulkCreatePlatformUserRoles
    request_path: "{{ sl_usermanagement_prefix }}/api/v1/platformUserRoles/bulkCreate"
    upstream_url: "{{ sl_usermanagement_service_url }}{{ sl_usermanagement_prefix }}/api/v1/platformUserRoles/bulkCreate"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: bulkUpdatePlatformUserRoles
    request_path: "{{ sl_usermanagement_prefix }}/api/v1/platformUserRoles/bulkUpdate"
    upstream_url: "{{ sl_usermanagement_service_url }}{{ sl_usermanagement_prefix }}/api/v1/platformUserRoles/bulkUpdate"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: bulkCreateUserRoles
    request_path: "{{ sl_usermanagement_prefix }}/api/v1/platformRoles/bulkCreate"
    upstream_url: "{{ sl_usermanagement_service_url }}{{ sl_usermanagement_prefix }}/api/v1/platformRoles/bulkCreate"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: bulkUpdateUserRoles
    request_path: "{{ sl_usermanagement_prefix }}/api/v1/platformRoles/bulkUpdate"
    upstream_url: "{{ sl_usermanagement_service_url }}{{ sl_usermanagement_prefix }}/api/v1/platformRoles/bulkUpdate"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: platformRolesList
    request_path: "{{ sl_usermanagement_prefix }}/api/v1/platformRoles/list"
    upstream_url: "{{ sl_usermanagement_service_url }}{{ sl_usermanagement_prefix }}/api/v1/platformRoles/list"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: punjabEncryptionService
    request_path: "{{ sl_usermanagement_prefix }}/api/v1/punjabSSO/encryptionService"
    upstream_url: "{{ sl_usermanagement_service_url }}{{ sl_usermanagement_prefix }}/api/v1/punjabSSO/encryptionService"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: punjabStaffForgotPassword
    request_path: "{{ sl_usermanagement_prefix }}/api/v1/punjabSSO/forgotPassword"
    upstream_url: "{{ sl_usermanagement_service_url }}{{ sl_usermanagement_prefix }}/api/v1/punjabSSO/forgotPassword"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: punjabStaffLogin
    request_path: "{{ sl_usermanagement_prefix }}/api/v1/punjabSSO/staffLogin"
    upstream_url: "{{ sl_usermanagement_service_url }}{{ sl_usermanagement_prefix }}/api/v1/punjabSSO/staffLogin"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: punjabStaffResetPassword
    request_path: "{{ sl_usermanagement_prefix }}/api/v1/punjabSSO/resetPassword"
    upstream_url: "{{ sl_usermanagement_service_url }}{{ sl_usermanagement_prefix }}/api/v1/punjabSSO/resetPassword"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: ssoLogin
    request_path: "{{ sl_usermanagement_prefix }}/api/v1/SSO/login"
    upstream_url: "{{ sl_usermanagement_service_url }}{{ sl_usermanagement_prefix }}/api/v1/SSO/login"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: userExtensionGetProfileUserManagement
    request_path: "{{ sl_usermanagement_prefix }}/api/v1/userExtension/getProfile"
    upstream_url: "{{ sl_usermanagement_service_url }}{{ sl_usermanagement_prefix }}/api/v1/userExtension/getProfile"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: userBelongscheck
    request_path: "{{ sl_sunbird_prefix }}/api/v1/bodh/abhyasa/userIsAllowed"
    upstream_url: "{{ sl_sunbird_service_url }}{{ sl_sunbird_prefix }}/api/v1/bodh/abhyasa/userIsAllowed"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: coursesEnrolled
    request_path: "{{ sl_sunbird_prefix }}/api/v1/bodh/batch/enrol"
    upstream_url: "{{ sl_sunbird_service_url }}{{ sl_sunbird_prefix }}/api/v1/bodh/batch/enrol"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: autoComplete
    request_path: "{{ sl_sunbird_prefix }}/api/v1/bodh/search/autocomplete"
    upstream_url: "{{ sl_sunbird_service_url }}{{ sl_sunbird_prefix }}/api/v1/bodh/search/autocomplete"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: middlewareBodhSearch
    request_path: "{{ sl_sunbird_prefix }}/api/v1/bodh/search/middleware"
    upstream_url: "{{ sl_sunbird_service_url }}{{ sl_sunbird_prefix }}/api/v1/bodh/search/middleware"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: middlewareBodhRequest
    request_path: "{{ sl_sunbird_prefix }}/api/v1/bodh/request/middleware"
    upstream_url: "{{ sl_sunbird_service_url }}{{ sl_sunbird_prefix }}/api/v1/bodh/request/middleware"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: generateQRCode
    request_path: "{{ sl_sunbird_prefix }}/api/v1/bodh/platform/generate"
    upstream_url: "{{ sl_sunbird_service_url }}{{ sl_sunbird_prefix }}/api/v1/bodh/platform/generate"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: uploadScromContent
    request_path: "{{ sl_sunbird_prefix }}/api/v1/bodh/platform/uploadScromContent"
    upstream_url: "{{ sl_sunbird_service_url }}{{ sl_sunbird_prefix }}/api/v1/bodh/platform/uploadScromContent"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: fetchDeviceProfile
    request_path: "{{ sl_sunbird_prefix }}/api/v1/device/profile"
    upstream_url: "{{ sl_sunbird_service_url }}{{ sl_sunbird_prefix }}/api/v1/device/profile"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: registerDeviceProfile
    request_path: "{{ sl_sunbird_prefix }}/api/v1/device/register"
    upstream_url: "{{ sl_sunbird_service_url }}{{ sl_sunbird_prefix }}/api/v1/device/register"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"


  - name: updateKeywords
    request_path: "{{ sl_sunbird_prefix }}/api/v1/dictionary/keywords/update"
    upstream_url: "{{ sl_sunbird_service_url }}{{ sl_sunbird_prefix }}/api/v1/dictionary/keywords/update"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: uploadKeywords
    request_path: "{{ sl_sunbird_prefix }}/api/v1/dictionary/keywords/upload"
    upstream_url: "{{ sl_sunbird_service_url }}{{ sl_sunbird_prefix }}/api/v1/dictionary/keywords/upload"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: generateToken
    request_path: "{{ sl_sunbird_prefix }}/api/v1/keycloak/generateToken"
    upstream_url: "{{ sl_sunbird_service_url }}{{ sl_sunbird_prefix }}/api/v1/keycloak/generateToken"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: filtersList
    request_path: "{{ sl_sunbird_prefix }}/api/v1/learningResources/filtersList"
    upstream_url: "{{ sl_sunbird_service_url }}{{ sl_sunbird_prefix }}/api/v1/learningResources/filtersList"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: listLearningResources
    request_path: "{{ sl_sunbird_prefix }}/api/v1/learningResources/list"
    upstream_url: "{{ sl_sunbird_service_url }}{{ sl_sunbird_prefix }}/api/v1/learningResources/list"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: assignRoles
    request_path: "{{ sl_sunbird_prefix }}/api/v1/organisations/assignRoles"
    upstream_url: "{{ sl_sunbird_service_url }}{{ sl_sunbird_prefix }}/api/v1/organisations/assignRoles"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: createOrgSlSunbird
    request_path: "{{ sl_sunbird_prefix }}/api/v1/organisations/create"
    upstream_url: "{{ sl_sunbird_service_url }}{{ sl_sunbird_prefix }}/api/v1/organisations/create"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: orgDetails
    request_path: "{{ sl_sunbird_prefix }}/api/v1/organisations/details"
    upstream_url: "{{ sl_sunbird_service_url }}{{ sl_sunbird_prefix }}/api/v1/organisations/details"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: listOrgs
    request_path: "{{ sl_sunbird_prefix }}/api/v1/organisations/list"
    upstream_url: "{{ sl_sunbird_service_url }}{{ sl_sunbird_prefix }}/api/v1/organisations/list"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: listOrgUsers
    request_path: "{{ sl_sunbird_prefix }}/api/v1/organisations/users"
    upstream_url: "{{ sl_sunbird_service_url }}{{ sl_sunbird_prefix }}/api/v1/organisations/users"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: activateOrg
    request_path: "{{ sl_sunbird_prefix }}/api/v1/organisations/activate"
    upstream_url: "{{ sl_sunbird_service_url }}{{ sl_sunbird_prefix }}/api/v1/organisations/activate"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: deactivateOrg
    request_path: "{{ sl_sunbird_prefix }}/api/v1/organisations/deactivate"
    upstream_url: "{{ sl_sunbird_service_url }}{{ sl_sunbird_prefix }}/api/v1/organisations/deactivate"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: removeuserFromOrg
    request_path: "{{ sl_sunbird_prefix }}/api/v1/organisations/removeUser"
    upstream_url: "{{ sl_sunbird_service_url }}{{ sl_sunbird_prefix }}/api/v1/organisations/removeUser"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: orgUpdate
    request_path: "{{ sl_sunbird_prefix }}/api/v1/organisations/update"
    upstream_url: "{{ sl_sunbird_service_url }}{{ sl_sunbird_prefix }}/api/v1/organisations/update"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: qrcodePdfs
    request_path: "{{ sl_sunbird_prefix }}/api/v1/qr-code/pdf"
    upstream_url: "{{ sl_sunbird_service_url }}{{ sl_sunbird_prefix }}/api/v1/qr-code/pdf"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: imageLinkofQrCode
    request_path: "{{ sl_sunbird_prefix }}/api/v1/qr-code/image"
    upstream_url: "{{ sl_sunbird_service_url }}{{ sl_sunbird_prefix }}/api/v1/qr-code/image"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: platformRoles
    request_path: "{{ sl_sunbird_prefix }}/api/v1/roles/list"
    upstream_url: "{{ sl_sunbird_service_url }}{{ sl_sunbird_prefix }}/api/v1/roles/list"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: verifyAccessToken
    request_path: "{{ sl_sunbird_prefix }}/api/v1/token/verify"
    upstream_url: "{{ sl_sunbird_service_url }}{{ sl_sunbird_prefix }}/api/v1/token/verify"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: activateUsers
    request_path: "{{ sl_sunbird_prefix }}/api/v1/users/activate"
    upstream_url: "{{ sl_sunbird_service_url }}{{ sl_sunbird_prefix }}/api/v1/users/activate"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: addUserToOrganisation
    request_path: "{{ sl_sunbird_prefix }}/api/v1/users/addUserToOrganisation"
    upstream_url: "{{ sl_sunbird_service_url }}{{ sl_sunbird_prefix }}/api/v1/users/addUserToOrganisation"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: usersCreateSlSunbird
    request_path: "{{ sl_sunbird_prefix }}/api/v1/users/create"
    upstream_url: "{{ sl_sunbird_service_url }}{{ sl_sunbird_prefix }}/api/v1/users/create"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: getProfile
    request_path: "{{ sl_sunbird_prefix }}/api/v1/users/getProfile"
    upstream_url: "{{ sl_sunbird_service_url }}{{ sl_sunbird_prefix }}/api/v1/users/getProfile"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: inactivateUsers
    request_path: "{{ sl_sunbird_prefix }}/api/v1/users/inactivate"
    upstream_url: "{{ sl_sunbird_service_url }}{{ sl_sunbird_prefix }}/api/v1/users/inactivate"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: libraryProjectDetails
    request_path: "{{ sl_unnati_prefix }}/api/v1/library/categories/projectDetails"
    upstream_url: "{{ unnati_service_url }}{{ sl_unnati_prefix }}/api/v1/library/categories/projectDetails"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: libraryCategories
    request_path: "{{ sl_unnati_prefix }}/api/v1/library/categories/list"
    upstream_url: "{{ unnati_service_url }}{{ sl_unnati_prefix }}/api/v1/library/categories/list"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: libraryProjects
    request_path: "{{ sl_unnati_prefix }}/api/v1/library/categories/projects"
    upstream_url: "{{ unnati_service_url }}{{ sl_unnati_prefix }}/api/v1/library/categories/projects"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"
  
  - name: bulkCreateProjects
    request_path: "{{ sl_unnati_prefix }}/api/v1/project/templateTasks/bulkCreate"
    upstream_url: "{{ unnati_service_url }}{{ sl_unnati_prefix }}/api/v1/project/templateTasks/bulkCreate"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: bulkUpdateProjects
    request_path: "{{ sl_unnati_prefix }}/api/v1/project/templateTasks/bulkUpdate"
    upstream_url: "{{ unnati_service_url }}{{ sl_unnati_prefix }}/api/v1/project/templateTasks/bulkUpdate"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: bulkCreateTemplates
    request_path: "{{ sl_unnati_prefix }}/api/v1/project/templates/bulkCreate"
    upstream_url: "{{ unnati_service_url }}{{ sl_unnati_prefix }}/api/v1/project/templates/bulkCreate"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: bulkUpdateTemplates
    request_path: "{{ sl_unnati_prefix }}/api/v1/project/templates/bulkUpdate"
    upstream_url: "{{ unnati_service_url }}{{ sl_unnati_prefix }}/api/v1/project/templates/bulkUpdate"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: importProjectTemplate
    request_path: "{{ sl_unnati_prefix }}/api/v1/project/templates/importProjectTemplate"
    upstream_url: "{{ unnati_service_url }}{{ sl_unnati_prefix }}/api/v1/project/templates/importProjectTemplate"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: entityReport
    request_path: "{{ sl_unnati_prefix }}/api/v1/reports/entity"
    upstream_url: "{{ unnati_service_url }}{{ sl_unnati_prefix }}/api/v1/reports/entity"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"


  - name: detailView
    request_path: "{{ sl_unnati_prefix }}/api/v1/reports/detailView"
    upstream_url: "{{ unnati_service_url }}{{ sl_unnati_prefix }}/api/v1/reports/detailView"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: getProgramsByEntity
    request_path: "{{ sl_unnati_prefix }}/api/v1/reports/getProgramsByEntity"
    upstream_url: "{{ unnati_service_url }}{{ sl_unnati_prefix }}/api/v1/reports/getProgramsByEntity"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: getReportTypes
    request_path: "{{ sl_unnati_prefix }}/api/v1/reports/types"
    upstream_url: "{{ unnati_service_url }}{{ sl_unnati_prefix }}/api/v1/reports/types"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: bulkCreateByUserRoleAndEntity
    request_path: "{{ sl_unnati_prefix }}/api/v1/userProjects/bulkCreateByUserRoleAndEntity"
    upstream_url: "{{ unnati_service_url }}{{ sl_unnati_prefix }}/api/v1/userProjects/bulkCreateByUserRoleAndEntity"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: bulkCreateUserProjects
    request_path: "{{ sl_unnati_prefix }}/api/v1/userProjects/bulkCreate"
    upstream_url: "{{ unnati_service_url }}{{ sl_unnati_prefix }}/api/v1/userProjects/bulkCreate"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: createProjects
    request_path: "{{ sl_unnati_prefix }}/api/v1/userProjects/create"
    upstream_url: "{{ unnati_service_url }}{{ sl_unnati_prefix }}/api/v1/userProjects/create"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: getFileUploadUrl
    request_path: "{{ sl_unnati_prefix }}/api/v1/userProjects/getFileUploadUrl"
    upstream_url: "{{ unnati_service_url }}{{ sl_unnati_prefix }}/api/v1/userProjects/getFileUploadUrl"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: importFromLibrary
    request_path: "{{ sl_unnati_prefix }}/api/v1/userProjects/importFromLibrary"
    upstream_url: "{{ unnati_service_url }}{{ sl_unnati_prefix }}/api/v1/userProjects/importFromLibrary"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: listofProjects
    request_path: "{{ sl_unnati_prefix }}/api/v1/userProjects/list"
    upstream_url: "{{ unnati_service_url }}{{ sl_unnati_prefix }}/api/v1/userProjects/list"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: projectDetails
    request_path: "{{ sl_unnati_prefix }}/api/v1/userProjects/details"
    upstream_url: "{{ unnati_service_url }}{{ sl_unnati_prefix }}/api/v1/userProjects/details"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: projectsMetaform
    request_path: "{{ sl_unnati_prefix }}/api/v1/userProjects/metaForm"
    upstream_url: "{{ unnati_service_url }}{{ sl_unnati_prefix }}/api/v1/userProjects/metaForm"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: tasksMetaForm
    request_path: "{{ sl_unnati_prefix }}/api/v1/userProjects/tasksMetaForm"
    upstream_url: "{{ unnati_service_url }}{{ sl_unnati_prefix }}/api/v1/userProjects/tasksMetaForm"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: projectSync
    request_path: "{{ sl_unnati_prefix }}/api/v1/userProjects/sync"
    upstream_url: "{{ unnati_service_url }}{{ sl_unnati_prefix }}/api/v1/userProjects/sync"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: tasksStatus
    request_path: "{{ sl_unnati_prefix }}/api/v1/userProjects/tasksStatus"
    upstream_url: "{{ unnati_service_url }}{{ sl_unnati_prefix }}/api/v1/userProjects/tasksStatus"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: solutionDetails
    request_path: "{{ sl_unnati_prefix }}/api/v1/userProjects/solutionDetails"
    upstream_url: "{{ unnati_service_url }}{{ sl_unnati_prefix }}/api/v1/userProjects/solutionDetails"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: userIsAllowed
    request_path: "{{ sl_kendra_prefix }}/api/v1/bodh/abhyasa/userIsAllowed"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/bodh/abhyasa/userIsAllowed"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: appsCreate
    request_path: "{{ sl_kendra_prefix }}/api/v1/apps/create"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/apps/create"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"       
       
  - name: getAppDetails
    request_path: "{{ sl_kendra_prefix }}/api/v1/apps/details"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/apps/details"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"       
       
  - name: updateAppDetails
    request_path: "{{ sl_kendra_prefix }}/api/v1/apps/update"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/apps/update"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"       
      
  - name: awsGetDownloadableUrl
    request_path: "{{ sl_kendra_prefix }}/api/v1/cloud-services/aws/getDownloadableUrl"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/cloud-services/aws/getDownloadableUrl"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"      
      
  - name: awsGetSignedUrl
    request_path: "{{ sl_kendra_prefix }}/api/v1/cloud-services/aws/preSignedUrls"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/cloud-services/aws/preSignedUrls"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"
      
  - name: azurePreSignedUrls
    request_path: "{{ sl_kendra_prefix }}/api/v1/cloud-services/azure/preSignedUrls"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/cloud-services/azure/preSignedUrls"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"      
      
  - name: AwsUploadFile
    request_path: "{{ sl_kendra_prefix }}/api/v1/cloud-services/aws/uploadFile"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/cloud-services/aws/uploadFile"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"      
      
  - name: azureGetDownloadableUrl
    request_path: "{{ sl_kendra_prefix }}/api/v1/cloud-services/azure/getDownloadableUrl"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/cloud-services/azure/getDownloadableUrl"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"            
      
  - name: azureUpload
    request_path: "{{ sl_kendra_prefix }}/api/v1/cloud-services/azure/upload"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/cloud-services/azure/upload"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"      
      
  - name: batchCoursesEnrolledByUsers
    request_path: "{{ sl_kendra_prefix }}/api/v1/bodh/batch/enrol"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/bodh/batch/enrol"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"      
      
  - name: bodhSearchAutocomplete
    request_path: "{{ sl_kendra_prefix }}/api/v1/bodh/search/autocomplete"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/bodh/search/autocomplete"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"      
      
  - name: bodhSearchMiddleware
    request_path: "{{ sl_kendra_prefix }}/api/v1/bodh/search/middleware"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/bodh/search/middleware"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: bodhRequestMiddleware
    request_path: "{{ sl_kendra_prefix }}/api/v1/bodh/request/middleware"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/bodh/request/middleware"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"
      
  - name: dictionaryKeywordsUpdate
    request_path: "{{ sl_kendra_prefix }}/api/v1/dictionary/keywords/update"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/dictionary/keywords/update"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"       
      
  - name: dictionaryKeywordsUpload
    request_path: "{{ sl_kendra_prefix }}/api/v1/dictionary/keywords/upload"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/dictionary/keywords/upload"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"      

  - name: UserRoles
    request_path: "{{ sl_kendra_prefix }}/api/v1/user-roles/list"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/user-roles/list"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: getPlatformUserRoles
    request_path: "{{ sl_kendra_prefix }}/api/v1/platform-user-roles/getProfile"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/platform-user-roles/getProfile"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"
      
  - name: sendEmailsToUsers
    request_path: "{{ sl_kendra_prefix }}/api/v1/email/jenkins"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/email/jenkins"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"       
      
  - name: entities
    request_path: "{{ sl_kendra_prefix }}/api/v1/entities/getUsersByEntityAndRole"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/entities/getUsersByEntityAndRole"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}" 
      
  - name: entitiesSubEntityTypeList
    request_path: "{{ sl_kendra_prefix }}/api/v1/entities/subEntityTypeList"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/entities/subEntityTypeList"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}" 
      
  - name: entitiesDetails
    request_path: "{{ sl_kendra_prefix }}/api/v1/entities/details"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/entities/details"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}" 
      
  - name: entitiesSubEntitiesRoles
    request_path: "{{ sl_kendra_prefix }}/api/v1/entities/subEntitiesRoles"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/entities/subEntitiesRoles"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}" 
      
  - name: entitiesSubEntityList
    request_path: "{{ sl_kendra_prefix }}/api/v1/entities/subEntityList"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/entities/subEntityList"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}" 
      
  - name: entitiesListByIds
    request_path: "{{ sl_kendra_prefix }}/api/v1/entities/listByIds"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/entities/listByIds"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}" 
      
  - name: listByEntityType
    request_path: "{{ sl_kendra_prefix }}/api/v1/entities/listByEntityType"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/entities/listByEntityType"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}" 
      
  - name: listByEntityTypeV2
    request_path: "{{ sl_kendra_prefix }}/api/v2/entities/listByEntityType"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v2/entities/listByEntityType"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"
      
  - name: entityTypesListKendra
    request_path: "{{ sl_kendra_prefix }}/api/v1/entity-types/list"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/entity-types/list"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}" 
      
  - name: formsDetails
    request_path: "{{ sl_kendra_prefix }}/api/v1/forms/details"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/forms/details"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}" 
      
  - name: gcpGetDownloadableUrl
    request_path: "{{ sl_kendra_prefix }}/api/v1/cloud-services/gcp/getDownloadableUrl"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/cloud-services/gcp/getDownloadableUrl"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}" 
      
  - name: gcpPreSignedUrls
    request_path: "{{ sl_kendra_prefix }}/api/v1/cloud-services/gcp/preSignedUrls"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/cloud-services/gcp/preSignedUrls"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}" 
      
  - name: gcpUploadFile
    request_path: "{{ sl_kendra_prefix }}/api/v1/cloud-services/gcp/uploadFile"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/cloud-services/gcp/uploadFile"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}" 
      
  - name: applicationConfigListLanguage
    request_path: "{{ sl_kendra_prefix }}/api/v1/application-config/listLanguage"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/application-config/listLanguage"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}" 
      
  - name: applicationConfigListAllLanguage
    request_path: "{{ sl_kendra_prefix }}/api/v1/application-config/listAllLanguages"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/application-config/listAllLanguages"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}" 
      
  - name: applicationConfigUploadLanguages
    request_path: "{{ sl_kendra_prefix }}/api/v1/application-config/uploadLanguages"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/application-config/uploadLanguages"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}" 
      
  - name: applicationConfigListConfigurations
    request_path: "{{ sl_kendra_prefix }}/api/v1/application-config/listConfigurations"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/application-config/listConfigurations"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}" 
      
  - name: applicationConfigUploadConfigurations
    request_path: "{{ sl_kendra_prefix }}/api/v1/application-config/uploadConfigurations"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/application-config/uploadConfigurations"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}" 
      
  - name: learningResourcesFilters
    request_path: "{{ sl_kendra_prefix }}/api/v1/learning-resources/filters"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/learning-resources/filters"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}" 
      
  - name: learningResourcesList
    request_path: "{{ sl_kendra_prefix }}/api/v1/learning-resources/list"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/learning-resources/list"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}" 
      
  - name: organisationsList
    request_path: "{{ sl_kendra_prefix }}/api/v1/organisations/list"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/organisations/list"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}" 
      
  - name: usersPrivateProgramsKendra
    request_path: "{{ sl_kendra_prefix }}/api/v1/users/privatePrograms"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/users/privatePrograms"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}" 
      
  - name: slackError
    request_path: "{{ sl_kendra_prefix }}/api/v1/slack/error"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/slack/error"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}" 
      
  - name: staticLinksListKendra
    request_path: "{{ sl_kendra_prefix }}/api/v1/static-links/list"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/static-links/list"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}" 
      
  - name: staticLinksBulkCreateKendra
    request_path: "{{ sl_kendra_prefix }}/api/v1/static-links/bulkCreate"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/static-links/bulkCreate"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"       
      
  - name: userExtensionGetEntities
    request_path: "{{ sl_kendra_prefix }}/api/v1/user-extension/getEntities"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/user-extension/getEntities"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"      
      
  - name: userExtensionGetEntitiesTypes
    request_path: "{{ sl_kendra_prefix }}/api/v1/user-extension/getEntityTypes"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/user-extension/getEntityTypes"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}" 
      
  - name: userExtensionGetProfileKendra
    request_path: "{{ sl_kendra_prefix }}/api/v2/user-extension/getProfile"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v2/user-extension/getProfile"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}" 
      
  - name: userExtensionUpdateProfileRoles
    request_path: "{{ sl_kendra_prefix }}/api/v1/user-extension/updateProfileRoles"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/user-extension/updateProfileRoles"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}" 
      
  - name: userExtensionUpdate
    request_path: "{{ sl_kendra_prefix }}/api/v1/user-extension/update"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/user-extension/update"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}" 
      
  - name: entitiesMappingForm
    request_path: "{{ sl_kendra_prefix }}/api/v1/users/entitiesMappingForm"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/users/entitiesMappingForm"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}" 
      
  - name: usersGetUserOrganisationsAndRootOrganisations
    request_path: "{{ sl_kendra_prefix }}/api/v1/users/getUserOrganisationsAndRootOrganisations"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/users/getUserOrganisationsAndRootOrganisations"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}" 
      
  - name: usersSearch
    request_path: "{{ sl_kendra_prefix }}/api/v1/users/search"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/users/search"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}" 
      
  - name: usersCreateProgram
    request_path: "{{ sl_kendra_prefix }}/api/v1/users/createProgram"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/users/createProgram"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}" 
      
  - name: usersIsSystemAdmin
    request_path: "{{ sl_kendra_prefix }}/api/v1/users/isSystemAdmin"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/users/isSystemAdmin"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}" 
      
  - name: usersCreateKendra
    request_path: "{{ sl_kendra_prefix }}/api/v1/users/create"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/users/create"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}" 
      
  - name: appReleasesCreate
    request_path: "{{ sl_kendra_prefix }}/api/v1/app-releases/create"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/app-releases/create"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"       
      
  - name: appReleasesList
    request_path: "{{ sl_kendra_prefix }}/api/v1/app-releases/list"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/app-releases/list"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"       
      
  - name: appReleasesUpdate
    request_path: "{{ sl_kendra_prefix }}/api/v1/app-releases/update"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/app-releases/update"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"       
      
  - name: appReleasesUpload
    request_path: "{{ sl_kendra_prefix }}/api/v1/app-releases/upload"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/app-releases/upload"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"       
           
  - name: cloudServicesAzureUploadFile
    request_path: "{{ sl_kendra_prefix }}/api/v1/cloud-services/azure/uploadFile"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/cloud-services/azure/uploadFile"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}" 
      
  - name: bodhPlatformGenerate
    request_path: "{{ sl_kendra_prefix }}/api/v1/bodh/platform/generate"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/bodh/platform/generate"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}" 
      
  - name: bodhPlatformUploadScromContent
    request_path: "{{ sl_kendra_prefix }}/api/v1/bodh/platform/uploadScromContent"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/bodh/platform/uploadScromContent"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}" 
      
  - name: notificationsInApp
    request_path: "{{ sl_kendra_prefix }}/api/v1/notifications/in-app/unReadCount"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/notifications/in-app/unReadCount"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}" 
      
  - name: notificationsInappList
    request_path: "{{ sl_kendra_prefix }}/api/v1/notifications/in-app/list"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/notifications/in-app/list"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}" 
      
  - name: notificationsInappMarkAsRead
    request_path: "{{ sl_kendra_prefix }}/api/v1/notifications/in-app/markAsRead"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/notifications/in-app/markAsRead"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}" 
      
  - name: notificationsPushToUsers
    request_path: "{{ sl_kendra_prefix }}/api/v1/notifications/push/pushToUsers"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/notifications/push/pushToUsers"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}" 
      
  - name: programsList
    request_path: "{{ sl_kendra_prefix }}/api/v1/programs/list"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/programs/list"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"       
      
  - name: notificationsPushToTopic
    request_path: "{{ sl_kendra_prefix }}/api/v1/notifications/push/pushToTopic"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/notifications/push/pushToTopic"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"       
            
  - name: notificationsPushRegisterDevice
    request_path: "{{ sl_kendra_prefix }}/api/v1/notifications/push/registerDevice"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/notifications/push/registerDevice"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"       
      
      
  - name: notificationsPushSubscribeToTopic
    request_path: "{{ sl_kendra_prefix }}/api/v1/notifications/push/subscribeToTopic"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/notifications/push/subscribeToTopic"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"       
      
  - name: notificationsPushUnsubscribeFromTopic
    request_path: "{{ sl_kendra_prefix }}/api/v1/notifications/push/unsubscribeFromTopic"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/notifications/push/unsubscribeFromTopic"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"       
      
  - name: getQrCodePdf
    request_path: "{{ sl_kendra_prefix }}/api/v1/qr-code/pdf"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/qr-code/pdf"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"       
      
  - name: getQrCodeImage
    request_path: "{{ sl_kendra_prefix }}/api/v1/qr-code/image"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/qr-code/image"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"       
            
  - name: userProfileGetForm
    request_path: "{{ sl_kendra_prefix }}/api/v1/user-profile/getForm"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/user-profile/getForm"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"       
      
  - name: userProfileSave
    request_path: "{{ sl_kendra_prefix }}/api/v1/user-profile/save"
    upstream_url: "{{ kendra_service_url }}{{ sl_kendra_prefix }}/api/v1/user-profile/save"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"


  - name: getStaticLinks
    request_path: "{{ sl_assessments_prefix }}/api/v2/staticLinks/list"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v2/staticLinks/list"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: solutionsMetaform
    request_path: "{{ sl_assessments_prefix }}/api/v1/assessments/metaForm/"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/assessments/metaForm/"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: createAssessmentSolution
    request_path: "{{ sl_assessments_prefix }}/api/v1/assessments/create"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/assessments/create"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"
  
  - name: detailedAssessments
    request_path: "{{ sl_assessments_prefix }}/api/v2/assessments/details/"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v2/assessments/details/"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"


  - name: navigationConfiguration
    request_path: "{{ sl_assessments_prefix }}/api/v1/configurations/navigation"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/configurations/navigation"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: createQuestions
    request_path: "{{ sl_assessments_prefix }}/api/v1/criteria/create"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/criteria/create"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: createQuestionsUpdate
    request_path: "{{ sl_assessments_prefix }}/api/v1/criteria/update"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/criteria/update"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: uploadCriteria
    request_path: "{{ sl_assessments_prefix }}/api/v1/criteria/upload"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/criteria/upload"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: bulkCreate
    request_path: "{{ sl_assessments_prefix }}/api/v1/entities/bulkCreate"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/entities/bulkCreate"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"    

  - name: bulkUpdate
    request_path: "{{ sl_assessments_prefix }}/api/v1/entities/bulkUpdate"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/entities/bulkUpdate"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}" 

  - name: entityAdd
    request_path: "{{ sl_assessments_prefix }}/api/v1/entities/add"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/entities/add"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}" 

  - name: entityForm
    request_path: "{{ sl_assessments_prefix }}/api/v1/entities/form"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/entities/form"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"     

  - name: entityList
    request_path: "{{ sl_assessments_prefix }}/api/v1/entities/list"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/entities/list"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: entityProfile
    request_path: "{{ sl_assessments_prefix }}/api/v1/entities/fetch"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/entities/fetch"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: relatedEntities
    request_path: "{{ sl_assessments_prefix }}/api/v1/entities/relatedEntities"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/entities/relatedEntities"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: updateEntity
    request_path: "{{ sl_assessments_prefix }}/api/v1/entities/update"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/entities/update"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"
      
  - name: uploadEntityForPortal
    request_path: "{{ sl_assessments_prefix }}/api/v1/entities/uploadForPortal"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/entities/uploadForPortal"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"    
      
  - name: uploadEntityMapping
    request_path: "{{ sl_assessments_prefix }}/api/v1/entities/mappingUpload"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/entities/mappingUpload"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"     
      
  - name: completedAssessments
    request_path: "{{ sl_assessments_prefix }}/api/v1/entityAssessors/completedAssessments"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/entityAssessors/completedAssessments"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"  

  - name: entityAssessors
    request_path: "{{ sl_assessments_prefix }}/api/v1/entityAssessors/entities"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/entityAssessors/entities"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"  
      
  - name: pendingAssessments
    request_path: "{{ sl_assessments_prefix }}/api/v1/entityAssessors/pendingAssessments"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/entityAssessors/pendingAssessments"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"   
      
  - name: uploadForPortal
    request_path: "{{ sl_assessments_prefix }}/api/v1/entityAssessors/uploadForPortal"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/entityAssessors/uploadForPortal"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"    
      
  - name: uploadForCSV
    request_path: "{{ sl_assessments_prefix }}/api/v1/entityAssessors/upload"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/entityAssessors/upload"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"    

  - name: createGroupEntityTypeIndex
    request_path: "{{ sl_assessments_prefix }}/api/v1/entityTypes/createGroupEntityTypeIndex"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/entityTypes/createGroupEntityTypeIndex"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"    

  - name: entityTypesCanBeObserved
    request_path: "{{ sl_assessments_prefix }}/api/v1/entityTypes/canBeObserved"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/entityTypes/canBeObserved"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"   
      
  - name: entityTypesList
    request_path: "{{ sl_assessments_prefix }}/api/v1/entityTypes/list"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/entityTypes/list"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"

  - name: exportFrameworkCriteria
    request_path: "{{ sl_assessments_prefix }}/api/v1/export/frameworkCriteria"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/export/frameworkCriteria"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"         
       
  - name: exportFramework
    request_path: "{{ sl_assessments_prefix }}/api/v1/export/framework"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/export/framework"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"
 
  - name: exportProgram
    request_path: "{{ sl_assessments_prefix }}/api/v1/export/program"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/export/program"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"      
      
  - name: exportSolutionCriteria
    request_path: "{{ sl_assessments_prefix }}/api/v1/export/solutionCriteria"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/export/solutionCriteria"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"     
      
  - name: exportSolution
    request_path: "{{ sl_assessments_prefix }}/api/v1/export/solution"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/export/solution"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"       
      
  - name: exportSolutionQuestions
    request_path: "{{ sl_assessments_prefix }}/api/v1/export/questions"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/export/questions"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}" 
      
  - name: filesGetImageUploadUrl
    request_path: "{{ sl_assessments_prefix }}/api/v1/files/getImageUploadUrl"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/files/getImageUploadUrl"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ small_request_size_limit }}"    
      
  - name: frameworksUpdate
    request_path: "{{ sl_assessments_prefix }}/api/v1/frameworks/update"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/frameworks/update"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"  
      
  - name: frameworksUploadThemes
    request_path: "{{ sl_assessments_prefix }}/api/v1/frameworks/uploadThemes"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/frameworks/uploadThemes"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"  
      
  - name: frameworksCreate
    request_path: "{{ sl_assessments_prefix }}/api/v1/frameworks/create"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/frameworks/create"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"  
 
  - name: importCriteria
    request_path: "{{ sl_assessments_prefix }}/api/v1/import/criteria"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/import/criteria"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"  
      
  - name: importFramework
    request_path: "{{ sl_assessments_prefix }}/api/v1/import/framework"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/import/framework"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"
      
  - name: importProgram
    request_path: "{{ sl_assessments_prefix }}/api/v1/import/program"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/import/program"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"              
      
      
  - name: importQuestions
    request_path: "{{ sl_assessments_prefix }}/api/v1/import/questions"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/import/questions"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"     
      
  - name: importSolution
    request_path: "{{ sl_assessments_prefix }}/api/v1/import/solution"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/import/solution"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"     
      
  - name: individualAssessmentsDetails
    request_path: "{{ sl_assessments_prefix }}/api/v1/library/individualAssessments/details"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/library/individualAssessments/details"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"           
      
  - name: individualAssessmentsList
    request_path: "{{ sl_assessments_prefix }}/api/v1/library/individualAssessments/list"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/library/individualAssessments/list"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"       
      
  - name: insightsGenerateFromSubmissionId
    request_path: "{{ sl_assessments_prefix }}/api/v1/insights/generateFromSubmissionId"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/insights/generateFromSubmissionId"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"       
      
  - name: insightsMultiEntityDrillDownReport
    request_path: "{{ sl_assessments_prefix }}/api/v1/insights/multiEntityDrillDownReport"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/insights/multiEntityDrillDownReport"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"      
      
      
  - name: insightsMultiEntityHighLevelReport
    request_path: "{{ sl_assessments_prefix }}/api/v1/insights/multiEntityHighLevelReport"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/insights/multiEntityHighLevelReport"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"       
            
  - name: insightsSingleEntityDrillDownReport
    request_path: "{{ sl_assessments_prefix }}/api/v1/insights/singleEntityDrillDownReport"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/insights/singleEntityDrillDownReport"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"
      
      
  - name: insightsSingleEntityHighLevelReport
    request_path: "{{ sl_assessments_prefix }}/api/v1/insights/singleEntityHighLevelReport"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/insights/singleEntityHighLevelReport"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"      

  - name: libraryInstitutionalAssessmentsDetails
    request_path: "{{ sl_assessments_prefix }}/api/v1/library/institutionalAssessments/details"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/library/institutionalAssessments/details"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"       
      
  - name: libraryInstitutionalAssessmentsList
    request_path: "{{ sl_assessments_prefix }}/api/v1/library/institutionalAssessments/list"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/library/institutionalAssessments/list"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"  
      
  - name: libraryCategoriesList
    request_path: "{{ sl_assessments_prefix }}/api/v1/library/categories/list"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/library/categories/list"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"    
      
  - name: librarySearchSolutions
    request_path: "{{ sl_assessments_prefix }}/api/v1/library/search/solutions"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/library/search/solutions"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"
      
  - name: mediaFilesCreateEmoji
    request_path: "{{ sl_assessments_prefix }}/api/v1/mediaFiles/createEmoji"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/mediaFiles/createEmoji"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}" 
            
  - name: mediaFilesCreateGesture
    request_path: "{{ sl_assessments_prefix }}/api/v1/mediaFiles/createGesture"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/mediaFiles/createGesture"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"   
          
      
  - name: mediaFilesGetAllEmojis
    request_path: "{{ sl_assessments_prefix }}/api/v1/mediaFiles/getAllEmojis"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/mediaFiles/getAllEmojis"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"        
      
  - name: mediaFilesGetAllGestures
    request_path: "{{ sl_assessments_prefix }}/api/v1/mediaFiles/getAllGestures"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/mediaFiles/getAllGestures"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"          
      
  - name: mediaFilesGetEmoji
    request_path: "{{ sl_assessments_prefix }}/api/v1/mediaFiles/getEmoji"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/mediaFiles/getEmoji"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"   
      
  - name: mediaFilesGetGesture
    request_path: "{{ sl_assessments_prefix }}/api/v1/mediaFiles/getGesture"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/mediaFiles/getGesture"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"      
            
  - name: libraryObservationsDetails
    request_path: "{{ sl_assessments_prefix }}/api/v1/library/observations/details"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/library/observations/details"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"      
                  
  - name: libraryObservationsList
    request_path: "{{ sl_assessments_prefix }}/api/v1/library/observations/list"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/library/observations/list"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"       
      
  - name: observationSubmissionsIsAllowed
    request_path: "{{ sl_assessments_prefix }}api/v1/observationSubmissions/isAllowed"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}api/v1/observationSubmissions/isAllowed"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"  
      
  - name: observationSubmissionsCreate
    request_path: "{{ sl_assessments_prefix }}/api/v1/observationSubmissions/create"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/observationSubmissions/create"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"       
           
      
  - name: observationSubmissionsMake
    request_path: "{{ sl_assessments_prefix }}/api/v1/observationSubmissions/make"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/observationSubmissions/make"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"      
      
      
  - name: observationSubmissionsDelete
    request_path: "{{ sl_assessments_prefix }}/api/v1/observationSubmissions/delete"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/observationSubmissions/delete"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"      
      
  - name: observationSubmissionsList
    request_path: "{{ sl_assessments_prefix }}/api/v1/observationSubmissions/list"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/observationSubmissions/list"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"       
      
  - name: pushCompletedObservationSubmissionForReporting
    request_path: "{{ sl_assessments_prefix }}/api/v1/observationSubmissions/pushCompletedObservationSubmissionForReporting"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/observationSubmissions/pushCompletedObservationSubmissionForReporting"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"       
               
  - name: pushInCompleteObservationSubmissionForReporting
    request_path: "{{ sl_assessments_prefix }}/api/v1/observationSubmissions/pushInCompleteObservationSubmissionForReporting"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/observationSubmissions/pushCompletedObservationSubmissionForReporting"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"       
                           
      
  - name: observationSubmissionsMultiRate
    request_path: "{{ sl_assessments_prefix }}/api/v1/observationSubmissions/multiRate"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/observationSubmissions/multiRate"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"       
            
  - name: observationSubmissionsSingleRate
    request_path: "{{ sl_assessments_prefix }}/api/v1/observationSubmissions/rate"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/observationSubmissions/rate"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"       

      
  - name: observationSubmissionsTitle
    request_path: "{{ sl_assessments_prefix }}/api/v1/observationSubmissions/title"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/observationSubmissions/title"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"   
 
  - name: observationAssessment
    request_path: "{{ sl_assessments_prefix }}/api/v2/observations/assessment"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v2/observations/assessment"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"   
  
 
  - name: observationBulkCreate
    request_path: "{{ sl_assessments_prefix }}/api/v1/observations/bulkCreate"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/observations/bulkCreate"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}" 
      
  - name: observationCompletedObservations
    request_path: "{{ sl_assessments_prefix }}/api/v1/observations/completedObservations"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/observations/completedObservations"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"       

  - name: observationCreate
    request_path: "{{ sl_assessments_prefix }}/api/v1/observations/create"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/observations/create"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}" 

  - name: observationsImportFromFramework
    request_path: "{{ sl_assessments_prefix }}/api/v1/observations/importFromFramework"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/observations/importFromFramework"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"
      
  - name: observationCreateV2
    request_path: "{{ sl_assessments_prefix }}/api/v2/observation/create"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v2/observation/create"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"
      
  - name: observationsDelete
    request_path: "{{ sl_assessments_prefix }}/api/v1/observations/delete"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/observations/delete"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"
      
  - name: observationsAddEntity
    request_path: "{{ sl_assessments_prefix }}/api/v1/observations/addEntityToObservation"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/observations/addEntityToObservation"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"
      
  - name: observationsSolutions
    request_path: "{{ sl_assessments_prefix }}/api/v1/observations/solutions"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/observations/solutions"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"
      
  - name: observationsDetails
    request_path: "{{ sl_assessments_prefix }}/api/v1/observations/details"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/observations/details"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"
      
  - name: observationsList
    request_path: "{{ sl_assessments_prefix }}/api/v2/observations/list"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v2/observations/list"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"
      
  - name: observationsPending
    request_path: "{{ sl_assessments_prefix }}/api/v1/observations/pendingObservations"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/observations/pendingObservations"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"
      
  - name: observationsSearchEntities
    request_path: "{{ sl_assessments_prefix }}/api/v2/observations/searchEntities"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}api/v2/observations/searchEntities"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"
      
  - name: observationsRemoveEntityFromObservation
    request_path: "{{ sl_assessments_prefix }}/api/v1/observations/removeEntityFromObservation"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/observations/removeEntityFromObservation"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"
      
  - name: observationsUpdate
    request_path: "{{ sl_assessments_prefix }}/api/v1/observations/update"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/observations/update"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"
      
  - name: pollSubmissionsMake
    request_path: "{{ sl_assessments_prefix }}/api/v1/pollSubmissions/make"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/pollSubmissions/make"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"
      
  - name: pollsCreate
    request_path: "{{ sl_assessments_prefix }}/api/v1/polls/create"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/polls/create"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"
      
  - name: pollsDelete
    request_path: "{{ sl_assessments_prefix }}/api/v1/polls/delete"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/polls/delete"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"
      
  - name: pollsGetPollQuestionsByLink
    request_path: "{{ sl_assessments_prefix }}/api/v1/polls/getPollQuestionsByLink"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/polls/getPollQuestionsByLink"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"
      
  - name: pollsGetPollQuestions
    request_path: "{{ sl_assessments_prefix }}/api/v1/polls/getPollQuestions"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/polls/getPollQuestions"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"
      
  - name: pollSubmissionsList
    request_path: "{{ sl_assessments_prefix }}/api/v1/pollSubmissions/list"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/pollSubmissions/list"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"
      
  - name: pollsMetaForm
    request_path: "{{ sl_assessments_prefix }}/api/v1/polls/metaForm"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/polls/metaForm"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"
      
  - name: programsBlockEntity
    request_path: "{{ sl_assessments_prefix }}/api/v1/programs/blockEntity"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/programs/blockEntity"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"
      
  - name: programsEntityList
    request_path: "{{ sl_assessments_prefix }}/api/v1/programs/entityList"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/programs/entityList"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"
      
  - name: programsUserEntityList
    request_path: "{{ sl_assessments_prefix }}/api/v1/programs/userEntityList"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/programs/userEntityList"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"
      
  - name: programsEntityBlocks
    request_path: "{{ sl_assessments_prefix }}/api/v1/programs/entityBlocks"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/programs/entityBlocks"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"
      
  - name: programsAllList
    request_path: "{{ sl_assessments_prefix }}/api/v1/programs/list"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/programs/list"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"
      
  - name: programOperationsAssessorReport
    request_path: "{{ sl_assessments_prefix }}/api/v1/programOperations/assessorReport"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/programOperations/assessorReport"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"
      
  - name: programOperationsEntityReport
    request_path: "{{ sl_assessments_prefix }}/api/v1/programOperations/entityReport"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/programOperations/entityReport"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"
      
  - name: programOperationsEntitySummary
    request_path: "{{ sl_assessments_prefix }}/api/v1/programOperations/entitySummary"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/programOperations/entitySummary"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"
      
  - name: programOperationsListByUser
    request_path: "{{ sl_assessments_prefix }}/api/v1/programOperations/listByUser"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/programOperations/listByUser"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"
            
  - name: programOperationsReportFilters
    request_path: "{{ sl_assessments_prefix }}/api/v1/programOperations/reportFilters"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/programOperations/reportFilters"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"      
      
  - name: programOperationsUserProfile
    request_path: "{{ sl_assessments_prefix }}/api/v1/programOperations/userProfile"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/programOperations/userProfile"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"      
      
  - name: programOperationssearchEntity
    request_path: "{{ sl_assessments_prefix }}/api/v1/programOperations/searchEntity"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/programOperations/searchEntity"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"      
      
  - name: usersPrivatePrograms
    request_path: "{{ sl_assessments_prefix }}/api/v1/users/privatePrograms"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/users/privatePrograms"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"     
      
      
  - name: questionsBulkUpdate
    request_path: "{{ sl_assessments_prefix }}/api/v1/questions/bulkUpdate"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/questions/bulkUpdate"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"      
      
  - name: questionsCreate
    request_path: "{{ sl_assessments_prefix }}/api/v1/questions/create"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/questions/create"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"     
      
  - name: questionsBulkCreate
    request_path: "{{ sl_assessments_prefix }}/api/v1/questions/bulkCreate"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/questions/bulkCreate"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"      
      
  - name: reportsEntityProfileInformation
    request_path: "{{ sl_assessments_prefix }}/api/v1/reports/entityProfileInformation"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/reports/entityProfileInformation"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"      
      
  - name: reportsEntityList
    request_path: "{{ sl_assessments_prefix }}/api/v1/reports/entityList"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/reports/entityList"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"      
      
  - name: reportsGenerateSubmissionReportsByEntityId
    request_path: "{{ sl_assessments_prefix }}/api/v1/reports/generateSubmissionReportsByEntityId"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/reports/generateSubmissionReportsByEntityId"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"      
      
  - name: reportsFrameworkDetails
    request_path: "{{ sl_assessments_prefix }}/api/v1/reports/frameworkDetails"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/reports/frameworkDetails"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"      

  - name: reportsAssessorEntities
    request_path: "{{ sl_assessments_prefix }}/api/v1/reports/assessorEntities"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/reports/assessorEntities"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"           
      
  - name: reportsGenerateCriteriaByEntityId
    request_path: "{{ sl_assessments_prefix }}/api/v1/reports/generateCriteriaByEntityId"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/reports/generateCriteriaByEntityId"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"      
      
  - name: reportsSolutionEntityStatus
    request_path: "{{ sl_assessments_prefix }}/api/v1/reports/solutionEntityStatus"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/reports/solutionEntityStatus"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"      
      
  - name: reportsEntityAssessors
    request_path: "{{ sl_assessments_prefix }}/api/v1/reports/entityAssessors"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/reports/entityAssessors"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"      
      
  - name: reportsSolutionsSubmissionStatus
    request_path: "{{ sl_assessments_prefix }}/api/v1/reports/solutionsSubmissionStatus"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/reports/solutionsSubmissionStatus"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"      
      
  - name: FetchSubmissionReports
    request_path: "{{ sl_assessments_prefix }}/api/v1/reports/status"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/reports/status"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"      
            
  - name: reportsEcmSubmissionByDate
    request_path: "{{ sl_assessments_prefix }}/api/v1/reports/ecmSubmissionByDate"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/reports/ecmSubmissionByDate"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"      
      
  - name: reportsGenerateEcmReportByDate
    request_path: "{{ sl_assessments_prefix }}/api/v1/reports/generateEcmReportByDate"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/reports/generateEcmReportByDate"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"      
      
  - name: reportsCompletedParentInterviewsByDate
    request_path: "{{ sl_assessments_prefix }}/api/v1/reports/completedParentInterviewsByDate"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/reports/completedParentInterviewsByDate"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"      
      
  - name: reportsSubmissionFeedback
    request_path: "{{ sl_assessments_prefix }}/api/v1/reports/submissionFeedback"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/reports/submissionFeedback"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"      
      
  - name: reportsParentInterviewCallResponseByDate
    request_path: "{{ sl_assessments_prefix }}/api/v1/reports/parentInterviewCallResponseByDate"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/reports/parentInterviewCallResponseByDate"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"            
      
  - name: reportsParentInterviewCallDidNotPickupReportByDate
    request_path: "{{ sl_assessments_prefix }}/api/v1/reports/parentInterviewCallDidNotPickupReportByDate"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/reports/parentInterviewCallDidNotPickupReportByDate"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"     
      
  - name: reportsRegistryDetails
    request_path: "{{ sl_assessments_prefix }}/api/v1/reports/registryDetails"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/reports/registryDetails"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"      
      
  - name: sharedLinksGenerate
    request_path: "{{ sl_assessments_prefix }}/api/v1/sharedLinks/generate"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/sharedLinks/generate"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"      
      
  - name: sharedLinksVerify
    request_path: "{{ sl_assessments_prefix }}/api/v1/sharedLinks/verify"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/sharedLinks/verify"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"      
      
  - name: solutionDetailsCriteria
    request_path: "{{ sl_assessments_prefix }}/api/v1/solutionDetails/criteria"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/solutionDetails/criteria"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"  
      
  - name: solutionDetailsEntities
    request_path: "{{ sl_assessments_prefix }}/api/v1/solutionDetails/entities"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/solutionDetails/entities"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"        
      
  - name: solutionDetailsQuestions
    request_path: "{{ sl_assessments_prefix }}/api/v1/solutionDetails/questions"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/solutionDetails/questions"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"        
      
  - name: solutionsGetObservationSolutionLink
    request_path: "{{ sl_assessments_prefix }}/api/v1/solutions/getObservationSolutionLink"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/solutions/getObservationSolutionLink"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"        
      
  - name: solutionsImportFromSolution
    request_path: "{{ sl_assessments_prefix }}/api/v1/solutions/importFromSolution"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/solutions/importFromSolution"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"        
      
  - name: solutionsImportFromFramework
    request_path: "{{ sl_assessments_prefix }}/api/v1/solutions/importFromFramework"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/solutions/importFromFramework"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"              
      
  - name: solutionsDelete
    request_path: "{{ sl_assessments_prefix }}/api/v1/solutions/delete"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/solutions/delete"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"        
      
      
  - name: FrameworkAndRubricSolutionsDetails
    request_path: "{{ sl_assessments_prefix }}/api/v1/solutions/details"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/solutions/details"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"        
      
  - name: solutionsMapEntityToSolution
    request_path: "{{ sl_assessments_prefix }}/api/v1/solutions/mapEntityToSolution"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/solutions/mapEntityToSolution"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"        
      
  - name: solutionsQuestionList
    request_path: "{{ sl_assessments_prefix }}/api/v1/solutions/questionList"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/solutions/questionList"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"        
            
  - name: solutionsMoveToTrash
    request_path: "{{ sl_assessments_prefix }}/api/v1/solutions/moveToTrash"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/solutions/moveToTrash"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"        
      
  - name: solutionsRemoveFromHomeScreen
    request_path: "{{ sl_assessments_prefix }}/api/v1/solutions/removeFromHomeScreen"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/solutions/removeFromHomeScreen"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"        
      
  - name: solutionsRestoreFromTrash
    request_path: "{{ sl_assessments_prefix }}/api/v1/solutions/restoreFromTrash"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/solutions/restoreFromTrash"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"        
      
  - name: solutionsTrashList
    request_path: "{{ sl_assessments_prefix }}/api/v1/solutions/trashList"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/solutions/trashList"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"              
      
  - name: solutionsUpdateSolutions
    request_path: "{{ sl_assessments_prefix }}/api/v1/solutions/updateSolutions"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/solutions/updateSolutions"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"        
      
  - name: solutionsUpdate
    request_path: "{{ sl_assessments_prefix }}/api/v1/solutions/update"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/solutions/update"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"        
      
  - name: solutionsUploadCriteriaRubricExpressions
    request_path: "{{ sl_assessments_prefix }}/api/v1/solutions/uploadCriteriaRubricExpressions"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/solutions/uploadCriteriaRubricExpressions"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"        
      
  - name: solutionsUploadThemesRubricExpressions
    request_path: "{{ sl_assessments_prefix }}/api/v1/solutions/uploadThemesRubricExpressions"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/solutions/uploadThemesRubricExpressions"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"        
      
  - name: solutionsUploadThemes
    request_path: "{{ sl_assessments_prefix }}/api/v1/solutions/uploadThemes"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/solutions/uploadThemes"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"        
      
  - name: solutionsVerifyLink
    request_path: "{{ sl_assessments_prefix }}/api/v1/solutions/verifyLink"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/solutions/verifyLink"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"        
                    
  - name: staticLinksList
    request_path: "{{ sl_assessments_prefix }}/api/v1/staticLinks/list"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/staticLinks/list"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"        
      
  - name: staticLinksBulkCreate
    request_path: "{{ sl_assessments_prefix }}/api/v1/staticLinks/bulkCreate"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/staticLinks/bulkCreate"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"        
      
  - name: submissionsGetCriteriaQuestions
    request_path: "{{ sl_assessments_prefix }}/api/v1/submissions/getCriteriaQuestions"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/submissions/getCriteriaQuestions"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"        
      
  - name: submissionsParentInterview
    request_path: "{{ sl_assessments_prefix }}/api/v1/submissions/parentInterview"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/submissions/parentInterview"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"        
      
  - name: submissionsCompleteParentInterview
    request_path: "{{ sl_assessments_prefix }}/api/v1/submissions/completeParentInterview"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/submissions/completeParentInterview"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"        
      
  - name: submissionsCreate
    request_path: "{{ sl_assessments_prefix }}/api/v1/submissions/create"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/submissions/create"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"        
      
  - name: submissionsDelete
    request_path: "{{ sl_assessments_prefix }}/api/v1/submissions/delete"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/submissions/delete"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"              
      
  - name: FetchSubmissionStatus
    request_path: "{{ sl_assessments_prefix }}/api/v1/submissions/status"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/submissions/status"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"        
      
  - name: FetchSubmission
    request_path: "{{ sl_assessments_prefix }}/api/v1/submissions/isAllowed"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/submissions/isAllowed"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"  
      
  - name: submissionsGeneralQuestions
    request_path: "{{ sl_assessments_prefix }}/api/v1/submissions/generalQuestions"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/submissions/generalQuestions"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"        
      
  - name: submissionsGetParentInterviewResponse
    request_path: "{{ sl_assessments_prefix }}/api/v1/submissions/getParentInterviewResponse"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/submissions/getParentInterviewResponse"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"        
      
  - name: submissionsList
    request_path: "{{ sl_assessments_prefix }}/api/v1/submissions/list"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/submissions/list"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"        
            
  - name: submissionsListImprovementProjectSuggestions
    request_path: "{{ sl_assessments_prefix }}/api/v1/submissions/listImprovementProjectSuggestions"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/submissions/listImprovementProjectSuggestions"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"        
      
  - name: submissionsMergeEcmSubmissionToAnswer
    request_path: "{{ sl_assessments_prefix }}/api/v1/submissions/mergeEcmSubmissionToAnswer"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/submissions/mergeEcmSubmissionToAnswer"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"        
      
  - name: submissionsPushCompletedSubmissionForReporting
    request_path: "{{ sl_assessments_prefix }}/api/v1/submissions/pushCompletedSubmissionForReporting"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/submissions/pushCompletedSubmissionForReporting"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"        
      
  - name: submissionsPushIncompleteSubmissionForReporting
    request_path: "{{ sl_assessments_prefix }}/api/v1/submissions/pushIncompleteSubmissionForReporting"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/submissions/pushIncompleteSubmissionForReporting"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"        
      
  - name: submissionsMultiRate
    request_path: "{{ sl_assessments_prefix }}/api/v1/submissions/multiRate"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/submissions/multiRate"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"              
      
  - name: submissionsRateEntity
    request_path: "{{ sl_assessments_prefix }}/api/v1/submissions/rate"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/submissions/rate"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"        
      
  - name: submissionsResetEcm
    request_path: "{{ sl_assessments_prefix }}/api/v1/submissions/resetEcm"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/submissions/resetEcm"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"        
      
  - name: submissionsSetTitle
    request_path: "{{ sl_assessments_prefix }}/api/v1/submissions/setTitle"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/submissions/setTitle"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"        
      
  - name: submissionsFeedbackAdded
    request_path: "{{ sl_assessments_prefix }}/api/v1/submissions/feedback"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/submissions/feedback"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"        
            
  - name: submissionsModifyByCsvUpload
    request_path: "{{ sl_assessments_prefix }}/api/v1/submissions/modifyByCsvUpload"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/submissions/modifyByCsvUpload"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"        
      
  - name: librarySurveysDetails
    request_path: "{{ sl_assessments_prefix }}/api/v1/library/surveys/details"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/library/surveys/details"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"        
      
  - name: librarySurveysList
    request_path: "{{ sl_assessments_prefix }}/api/v1/library/surveys/list"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/library/surveys/list"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"        
      
  - name: surveySubmissionsGetStatus
    request_path: "{{ sl_assessments_prefix }}/api/v1/surveySubmissions/getStatus"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/surveySubmissions/getStatus"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"        
      
  - name: checkSubmissionsStatus
    request_path: "{{ sl_assessments_prefix }}/api/v1/surveySubmissions/isAllowed"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/surveySubmissions/isAllowed"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"        
      
  - name: createSurveySubmission
    request_path: "{{ sl_assessments_prefix }}/api/v1/surveySubmissions/make"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/surveySubmissions/make"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"        
            
  - name: surveySubmissionsList
    request_path: "{{ sl_assessments_prefix }}/api/v1/surveySubmissions/list"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/surveySubmissions/list"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"        
      
  - name: surveysBulkCreate
    request_path: "{{ sl_assessments_prefix }}/api/v1/surveys/bulkCreate"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/surveys/bulkCreate"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"        
      
  - name: surveysCreateSolutionTemplate
    request_path: "{{ sl_assessments_prefix }}/api/v1/surveys/createSolutionTemplate"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/surveys/createSolutionTemplate"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"        
      
  - name: surveysGetDetailsByLink
    request_path: "{{ sl_assessments_prefix }}/api/v1/surveys/getDetailsByLink"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/surveys/getDetailsByLink"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"        
      
  - name: surveysImportSurveryTemplateToSolution
    request_path: "{{ sl_assessments_prefix }}/api/v1/surveys/importSurveryTemplateToSolution"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/surveys/importSurveryTemplateToSolution"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"        
      
  - name: surveysMapSurverySolutionToProgram
    request_path: "{{ sl_assessments_prefix }}/api/v1/surveys/mapSurverySolutionToProgram"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/surveys/mapSurverySolutionToProgram"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"        
      
  - name: surveysDetails
    request_path: "{{ sl_assessments_prefix }}/api/v1/surveys/details"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/surveys/details"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"        
      
  - name: userExtensionBulkUpload
    request_path: "{{ sl_assessments_prefix }}/api/v1/userExtension/bulkUpload"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/userExtension/bulkUpload"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"        
      
  - name: userExtensionGetProfile
    request_path: "{{ sl_assessments_prefix }}/api/v1/userExtension/getProfile"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/userExtension/getProfile"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"        
      
  - name: userExtensionEntities
    request_path: "{{ sl_assessments_prefix }}/api/v1/userExtension/entities"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/userExtension/entities"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"        
      
  - name: userRolesBulkCreate
    request_path: "{{ sl_assessments_prefix }}/api/v1/userRoles/bulkCreate	"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/userRoles/bulkCreate"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"        
      
  - name: userRolesBulkUpdate
    request_path: "{{ sl_assessments_prefix }}/api/v1/userRoles/bulkUpdate"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/userRoles/bulkUpdate"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"        
      
  - name: userRolesList
    request_path: "{{ sl_assessments_prefix }}/api/v1/userRoles/list"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/userRoles/list"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"              
      
  - name: listOfUserEntities
    request_path: "{{ sl_assessments_prefix }}/api/v1/users/entities"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/users/entities"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"        
      
  - name: listOfUserPrograms
    request_path: "{{ sl_assessments_prefix }}/api/v1/users/programs"
    upstream_url: "{{ sl_assessments_service_url }}{{ sl_assessments_prefix }}/api/v1/users/programs"
    strip_request_path: true
    plugins:
    -  name: jwt
    - name: cors
    - "{{ statsd_pulgin }}" 
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting 
      config.allowed_payload_size: "{{ small_request_size_limit }}"

