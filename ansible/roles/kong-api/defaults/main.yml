---
# Guideline for PR Merging: https://project-sunbird.atlassian.net/wiki/spaces/DevOps/pages/1210777612/API+Manager+Kong+BEST+Practices+Documentation.

# OnboardAPI's: https://project-sunbird.atlassian.net/wiki/spaces/DevOps/pages/1205207131/Best+Practces+For+Onboarding+API+s+In+Sunbird

# Onboard Consumers: https://project-sunbird.atlassian.net/wiki/spaces/DevOps/pages/1202847861/Best+Practces+For+Onboarding+Consumers+In+Sunbird
kong_admin_api_url: "http://{{kong_host}}:8001"

# Default statsd_metrics
statsd_host: statsd-service
statsd_port: 9125

statsd_pulgin:
  name: statsd
  config.host: "{{ statsd_host }}"
  config.port: "{{ statsd_port }}"

# Default Rate limits
small_rate_limit_per_hour: 100
medium_rate_limit_per_hour: 5000
x_medium_rate_limit_per_hour: 5000
large_rate_limit_per_hour: 10000
x2_large_rate_limit_per_hour: 15000
x_large_rate_limit_per_hour: 30000

premium_consumer_small_rate_limit_per_hour: 1000
premium_consumer_medium_rate_limit_per_hour: 10000
premium_consumer_large_rate_limit_per_hour: 100000

# Default max request size in MB
medium_request_size_limit: 1
medium_request_size_limit: 10
large_request_size_limit: 100

# External URL's
freshDesk_url: "http://dummy.freshDesk.url"

# Default service prefixes
learner_private_route_prefix: /private
private_content_prefix: /private/content
device_prefix: /v3/device
content_prefix: /content
tickets_prefix: /tickets
course_service_prefix: /course
itemset_prefix: /itemset
user_service_prefix: /user
data_service_prefix: /data
notes_service_prefix: /notes
org_service_prefix: /org
echo_service_prefix: /echo
composite_service_prefix: /composite
api_manager_perfix: /api-manager
meta_service_prefix: /meta
announcement_service_prefix: /announcement
dialcode_service_prefix: /dialcode
channel_service_prefix: /channel
framework_service_prefix: /framework
badge_service_prefix: /badging
plugin_service_prefix: /plugins
config_service_prefix: /config
textbook_service_prefix: /textbook
lock_service_prefix: /lock
otp_service_prefix: /otp
sso_service_prefix: /sso
cert_service_prefix: /cert
cert_registry_service_prefix: /certreg
desktop_app_prefix: /desktop
license_api_prefix: /license
report_service_prefix: /data/v1/report-service
analytics_report_service_prefix: /report/jobs
group_service_prefix: /group
auth_service_prefix: /auth
object_category_prefix: /object/category
object_category_definition_prefix: /object/category/definition
dataset_service_prefix: /dataset
asset_prefix: /asset
collection_prefix: /collection
discussions_prefix: /discussion
question_prefix: /question
questionset_prefix: /questionset


# Service URLs
knowledge_mw_service_url: "http://knowledge-mw-service:5000"
learning_service_url: "http://learner-service:9000"
vm_learning_service_url: "http://{{learningservice_ip}}:8080/learning-service"
telemetry_service_url: "http://telemetry-service:9001"
player_service_url: "http://player:3000"
echo_service_url: "http://echo:9595"
am_util_url: "http://adminutil:4000"
config_service_url: "http://config-service:8080"
lms_service_url: "http://lms-service:9000"
cert_service_url: "http://cert-service:9000"
cert_registry_service_url: "http://cert-registry-service:9000"
content_service_url: "http://content-service:9000"
report_service_url: "http://report-service:3030"
group_service_url: "http://groups-service:9000"
analytics_api_service_url: "http://analytics-service:9000"
taxonomy_service_url: "http://taxonomy-service:9000"
discussions_mw_service_url: "http://discussionsmw-service:3002/discussion"
assessment_service_url: "http://assessment-service:9000"

premium_consumer_rate_limits:
  - api: createContent
    config.hour: "{{ premium_consumer_small_rate_limit_per_hour }}"
    config.limit_by: credential
    config.policy: local
  - api: updateContent
    config.hour: "{{ premium_consumer_medium_rate_limit_per_hour }}"
    config.limit_by: credential
    config.policy: local
  - api: readContent
    config.hour: "{{ premium_consumer_large_rate_limit_per_hour }}"
    config.limit_by: credential
    config.policy: local

kong_apis:

  - name: PROGRAM_LISTING
    Request_path: "{{ content_prefix }}/v1/users/programs"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/users/programs"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"

  - name: GET_PROJECTS
    Request_path: "{{ content_prefix }}/v1/userProjects/getProject"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/userProjects/getProject"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: SOLUTIONS_LISTING
    Request_path: "{{ content_prefix }}/v1/users/solutions"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/users/solutions"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: GET_PROJECT
    Request_path: "{{ content_prefix }}/v2/userProjects/details"
    upstream_url: "{{ knowledge_mw_service_url }}/v2/userProjects/details"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: NOTIFICATIONs_LIST
    Request_path: "{{ content_prefix }}/v1/notifications/in-app/list"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/notifications/in-app/list"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
      
  - name: NOTIFICATION_COUNT
    Request_path: "{{ content_prefix }}/v1/notifications/in-app/unReadCount"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/notifications/in-app/unReadCount"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: PROJECTS_LIST
    Request_path: "{{ content_prefix }}/v1/userProjects/list"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/userProjects/list"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
      
  - name: LIBRARY_CATEGORIES
    Request_path: "{{ content_prefix }}/v1/library/categories/list"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/library/categories/list"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: TEMPLATES_LIST
    Request_path: "{{ content_prefix }}/v1/library/categories/projects"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/library/categories/projects"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
      
  - name: TEMPLATE_DATA
    Request_path: "{{ content_prefix }}/v1/library/categories/projectDetails"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/library/categories/projectDetails"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
      
  - name: CREATE_PROJECT_FORM
    Request_path: "{{ content_prefix }}/v1/userProjects/metaForm"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/userProjects/metaForm"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
      
  - name: TEMPLATE_SORT
    Request_path: "{{ content_prefix }}/v1/library/categories/projects"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/library/categories/projects"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
      
  - name: REGISTERDEVICE
    Request_path: "{{ content_prefix }}/v1/notifications/push/registerDevice"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/notifications/push/registerDevice"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
      
  - name: PRIVATE_PROGRAMS
    Request_path: "{{ content_prefix }}/v1/users/privatePrograms"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/users/privatePrograms"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
      
  - name: GET_PROFILE
    Request_path: "{{ content_prefix }}/v2/user-extension/getProfile"
    upstream_url: "{{ knowledge_mw_service_url }}/v2/user-extension/getProfile"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: GET_STATES
    Request_path: "{{ content_prefix }}/v2/entities/listByEntityType/state"
    upstream_url: "{{ knowledge_mw_service_url }}/v2/entities/listByEntityType/state"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: GET_SUBENTITIES
    Request_path: "{{ content_prefix }}/v1/entities/subEntityTypeList"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/entities/subEntityTypeList"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: GET_ENTITY_LIST
    Request_path: "{{ content_prefix }}/v1/entities/subEntityList"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/entities/subEntityList"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: IMPORT_TEMPLATE
    Request_path: "{{ content_prefix }}/v1/userProjects/importFromLibrary"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/userProjects/importFromLibrary"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: GET_REPORT
    Request_path: "{{ content_prefix }}/v1/reports/entity"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/reports/entity"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: GET_REPORT_TYPES
    Request_path: "{{ content_prefix }}/v1/reports/types"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/reports/types"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: GET_ENTITY_TYPES
    Request_path: "{{ content_prefix }}/v1/user-extension/getEntityTypes"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/user-extension/getEntityTypes"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: GET_ENTITIES_BY_TYPE
    Request_path: "{{ content_prefix }}/v1/user-extension/getEntities"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/user-extension/getEntities"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: GET_FULL_REPORT
    Request_path: "{{ content_prefix }}/v1/reports/detailView"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/reports/detailView"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: GET_PROGRAM_BY_ENTITY 
    Request_path: "{{ content_prefix }}/v1/reports/getProgramsByEntity"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/reports/getProgramsByEntity"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: GET_TASK_META_FORM
    Request_path: "{{ content_prefix }}/v1/userProjects/tasksMetaForm"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/userProjects/tasksMetaForm"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: GET_LEARNING_RESOURCES
    Request_path: "{{ content_prefix }}/v1/learningResources/list"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/learningResources/list"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: LEARNING_RESOURCES_LIST
    Request_path: "{{ content_prefix }}/v1/learning-resources/list?"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/learning-resources/list?"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: GET_DYNAMIC_LINKS
    Request_path: "{{ content_prefix }}/v1/static-links/list"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/static-links/list"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: GET_LEARNING_RESOURCES_FILTERS
    Request_path: "{{ content_prefix }}/v1/learning-resources/filters"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/learning-resources/filters"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: PROFILE_INFO
    Request_path: "{{ content_prefix }}/v1/users/getProfile"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/users/getProfile"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: SYNC_PROJECT
    Request_path: "{{ content_prefix }}/v1/userProjects/sync"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/userProjects/sync"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"      

  - name: CREATE_PROJECT_DOC 
    Request_path: "{{ content_prefix }}/v1/userProjects/create"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/userProjects/create"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: GET_IMAGE_UPLOAD_URLS
    Request_path: "{{ content_prefix }}/v1/userProjects/getFileUploadUrl"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/userProjects/getFileUploadUrl"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: START_ASSESSMENT
    Request_path: "{{ content_prefix }}/v1/userProjects/solutionDetails"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/userProjects/solutionDetails"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: PROJCET_TASK_STATUS
    Request_path: "{{ content_prefix }}/v1/userProjects/tasksStatus"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/userProjects/tasksStatus"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
      
  - name: LIBRARY_SEARCH
    Request_path: "{{ content_prefix }}/v1/library/categories/projects"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/library/categories/projects"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: STATE_LIST
    Request_path: "{{ content_prefix }}/v1/entities/listByEntityType/state"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/entities/listByEntityType/state"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
      
  - name: STATE_ROLES
    Request_path: "{{ content_prefix }}/v1/entities/subEntitiesRoles"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/entities/subEntitiesRoles"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: ENTITY_MAPPING_FORM
    Request_path: "{{ content_prefix }}/v1/users/entitiesMappingForm"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/users/entitiesMappingForm"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
      
  - name: PROFILE_UPDATE
    Request_path: "{{ content_prefix }}/v1/user-extension/updateProfileRoles"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/user-extension/updateProfileRoles"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
      
  - name: MARK_AS_READ
    Request_path: "{{ content_prefix }}/v1/notifications/in-app/markAsRead"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/notifications/in-app/markAsRead"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
      
  - name: GET_PREVIOUS_PROFILE
    Request_path: "{{ content_prefix }}/v1/users/search"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/users/search"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
      
  - name: GET_PROG_SOL_FOR_OBSERVATION
    Request_path: "{{ content_prefix }}/v1/observations/getObservation"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/observations/getObservation"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
      
  - name: GET_OBSERVATION_ENTITIES
    Request_path: "{{ content_prefix }}/v1/observations/entities"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/observations/entities"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
      
  - name: GET_OBSERVATION_SUBMISSIONS
    Request_path: "{{ content_prefix }}/v1/observationSubmissions/list"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/observationSubmissions/list"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: GET_OBSERVATION_DETAILS
    Request_path: "{{ content_prefix }}/v2/observations/assessment"
    upstream_url: "{{ knowledge_mw_service_url }}/v2/observations/assessment"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: MANDATORY_ENTITY_TYPES_FOR_ROLES
    Request_path: "{{ content_prefix }}/v1/users/entityTypesByLocationAndRole"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/users/entityTypesByLocationAndRole"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: GET_OBSERVATION_SUBMISSION_COUNT
    Request_path: "{{ content_prefix }}/v1/observations/submissionsCount"
    upstream_url: "{{ knowledge_mw_service_url }}v1/observations/submissionsCount"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: EDIT_OBSERVATION_NAME
    Request_path: "{{ content_prefix }}/v1/observationSubmissions/title"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/observationSubmissions/title"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: OBSERVATION_SUBMISSION_DELETE
    Request_path: "{{ content_prefix }}/v1/observationSubmissions/delete"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/observationSubmissions/delete"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: OBSERVATION_SUBMISSION_CREATE
    Request_path: "{{ content_prefix }}/v1/observationSubmissions/create"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/observationSubmissions/create"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: ENTITY_LIST_BASED_ON_ENTITY_TYPE
    Request_path: "{{ content_prefix }}/v1/entities/listByEntityType"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/entities/listByEntityType"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: SEARCH_ENTITY
    Request_path: "{{ content_prefix }}/v2/observations/searchEntities"
    upstream_url: "{{ knowledge_mw_service_url }}/v2/observations/searchEntities"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: MAP_ENTITY_TO_OBSERVATION
    Request_path: "{{ content_prefix }}/v1/observations/addEntityToObservation"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/observations/addEntityToObservation"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: UNMAP_ENTITY_TO_OBSERVATION
    Request_path: "{{ content_prefix }}/v1/observations/removeEntityFromObservation"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/observations/removeEntityFromObservation"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: IS_SURVEY_SUBMISSION_ALLOWED
    Request_path: "{{ content_prefix }}/v1/surveySubmissions/isAllowed"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/surveySubmissions/isAllowed"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: IS_OBSERVATION_SUBMISSION_ALLOWED
    Request_path: "{{ content_prefix }}/v1/observationSubmissions/isAllowed"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/observationSubmissions/isAllowed"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: CHECK_IF_SUBMITTED 
    Request_path: "{{ content_prefix }}/v1/submissions/isAllowed"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/submissions/isAllowed"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: SURVEY_FEEDBACK_MAKE_SUBMISSION
    Request_path: "{{ content_prefix }}/v1/surveySubmissions/make"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/surveySubmissions/make"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: OBSERVATION_MAKE_SUBMISSION
    Request_path: "{{ content_prefix }}/v1/observationSubmissions/make"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/observationSubmissions/make"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: SUBMISSION
    Request_path: "{{ content_prefix }}/v1/submissions/make"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/submissions/make"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: GET_SURVEY_IMAGE_UPLOAD_URLS
    Request_path: "{{ content_prefix }}/v1/files/getImageUploadUrl"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/files/getImageUploadUrl"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"        
      
  - name: GET_SUBMISSION_PDF
    Request_path: "{{ content_prefix }}/v1/observationSubmissions/pdfFileUrl"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/observationSubmissions/pdfFileUrl"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: OBSERVATION_REPORTS_INSTANCE_REPORT
    Request_path: "{{ content_prefix }}/observations/instance"
    upstream_url: "{{ knowledge_mw_service_url }}/observations/instance"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: OBSERVATION_REPORTS_ENTITY_REPORT
    Request_path: "{{ content_prefix }}/observations/entity"
    upstream_url: "{{ knowledge_mw_service_url }}/observations/entity"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: OBSERVATION_REPORT
    Request_path: "{{ content_prefix }}/observations/report"
    upstream_url: "{{ knowledge_mw_service_url }}/observations/report"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
      
  - name: OBSERVATION_REPORTS_GET_REPORTS_PDF_URLS
    Request_path: "{{ content_prefix }}/observations/pdfReports"
    upstream_url: "{{ knowledge_mw_service_url }}/observations/pdfReports"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: OBSERVATION_REPORTS_OBSERVATION_LIST
    Request_path: "{{ content_prefix }}/observations/listObservationNames"
    upstream_url: "{{ knowledge_mw_service_url }}/observations/listObservationNames"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
      
  - name: OBSERVATION_REPORTS_ENTITY_OBSERVATION_REPORT
    Request_path: "{{ content_prefix }}/observations/entityObservationReport"
    upstream_url: "{{ knowledge_mw_service_url }}/observations/entityObservationReport"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: OBSERVATION_REPORTS_SOLUTIOIN_LIST
    Request_path: "{{ content_prefix }}/observations/listObservationSolutions"
    upstream_url: "{{ knowledge_mw_service_url }}/observations/listObservationSolutions"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
      
  - name: OBSERVATION_REPORTS_ENTITY_SOLUTION_REPORT
    Request_path: "{{ content_prefix }}/observations/entitySolutionReport"
    upstream_url: "{{ knowledge_mw_service_url }}observations/entitySolutionReport"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
      
  - name: OBSERVATION_REPORTS_ALL_EVIDENCE
    Request_path: "{{ content_prefix }}/v1/observations/listAllEvidences"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/observations/listAllEvidences"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
      
  - name: OBSERVATION_REPORTS_WITH_SCORE_INSTANCE_REPORT
    Request_path: "{{ content_prefix }}/observations/instanceObservationScoreReport"
    upstream_url: "{{ knowledge_mw_service_url }}observations/instanceObservationScoreReport"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
      
  - name: OBSERVATION_REPORTS_WITH_SCORE_ENTITY_REPORT
    Request_path: "{{ content_prefix }}/observations/entityScoreReport"
    upstream_url: "{{ knowledge_mw_service_url }}/observations/entityScoreReport"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
      
  - name: OBSERVATION_REPORTS_WITH_SCORE_OBSERVATION_REPORT
    Request_path: "{{ content_prefix }}/observations/scoreReport"
    upstream_url: "{{ knowledge_mw_service_url }}/observations/scoreReport"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"

  - name: OBSERVATION_REPORTS_WITH_SCORE_GET_REPORTS_PDF_URLS
    Request_path: "{{ content_prefix }}/observations/observationScorePdfReport"
    upstream_url: "{{ knowledge_mw_service_url }}/observations/observationScorePdfReport"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: OBSERVATION_REPORTS_WITH_SCORE_OBSERVATION_LIST
    Request_path: "{{ content_prefix }}/observations/listObservationNames"
    upstream_url: "{{ knowledge_mw_service_url }}/observations/listObservationNames"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: OBSERVATION_REPORTS_WITH_SCORE_SOLUTION_REPORT
    Request_path: "{{ content_prefix }}/observations/entitySolutionScoreReport"
    upstream_url: "{{ knowledge_mw_service_url }}/observations/entitySolutionScoreReport"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: CRITERIA_REPORTS_WITH_SCORE_INSTANCE_REPORT 
    Request_path: "{{ content_prefix }}/v1/observations/instanceScoreReportByCriteria"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/observations/instanceScoreReportByCriteria"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: CRITERIA_REPORTS_WITH_SCORE_ENTITY_REPORT
    Request_path: "{{ content_prefix }}/v1/observations/entityScoreReportByCriteria"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/observations/entityScoreReportByCriteria"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: CRITERIA_REPORTS_WITH_SCORE_OBSERVATION_REPORT
    Request_path: "{{ content_prefix }}/v1/observations/observationScoreReportByCriteria"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/observations/observationScoreReportByCriteria"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: CRITERIA_REPORTS_WITH_SCORE_GET_REPORTS_PDF_URLS
    Request_path: "{{ content_prefix }}/observations/scorePdfReportsByCriteria"
    upstream_url: "{{ knowledge_mw_service_url }}/observations/scorePdfReportsByCriteria"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: CRITERIA_REPORTS_INSTANCE_REPORT
    Request_path: "{{ content_prefix }}/v1/observations/instanceReportByCriteria"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/observations/instanceReportByCriteria"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: CRITERIA_REPORTS_ENTITY_REPORT
    Request_path: "{{ content_prefix }}/v1/observations/entityReportByCriteria"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/observations/entityReportByCriteria"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: CRITERIA_REPORTS_OBSERVATION_REPORT
    Request_path: "{{ content_prefix }}/v1/observations/observationReportByCriteria"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/observations/observationReportByCriteria"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: CRITERIA_REPORTS_GET_REPORTS_PDF_URLS
    Request_path: "{{ content_prefix }}/observations/pdfReportsByCriteria"
    upstream_url: "{{ knowledge_mw_service_url }}/observations/pdfReportsByCriteria"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: SURVEY_FEEDBACK_SURVEY_LISTING
    Request_path: "{{ content_prefix }}/v1/surveys/getSurvey"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/surveys/getSurvey"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: SURVEY_FEEDBACK_GET_DETAILS_BY_LINK
    Request_path: "{{ content_prefix }}/v1/surveys/getDetailsByLink"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/surveys/getDetailsByLink"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: SURVEY_FEEDBACK_GET_DETAILS_BY_ID
    Request_path: "{{ content_prefix }}/v2/surveys/details"
    upstream_url: "{{ knowledge_mw_service_url }}/v2/surveys/details"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: SURVEY_FEEDBACK_MAKE_SUBMISSION
    Request_path: "{{ content_prefix }}/v1/surveySubmissions/make"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/surveySubmissions/make"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: SURVEY_FEEDBACK_SUBMISSION_REPORT
    Request_path: "{{ content_prefix }}/v1/surveys/submissionReport"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/surveys/submissionReport"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: SURVEY_FEEDBACK_SOLUTION_REPORT
    Request_path: "{{ content_prefix }}/v1/surveys/solutionReport"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/surveys/solutionReport"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: SURVEY_FEEDBACK_IS_SUBMISSION_ALLOWED
    Request_path: "{{ content_prefix }}/v1/surveySubmissions/isAllowed"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/surveySubmissions/isAllowed"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"        
      
  
  - name: SURVEY_FEEDBACK_LIST_ALL_EVIDENCES
    Request_path: "{{ content_prefix }}/v1/surveys/listAllEvidences"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/surveys/listAllEvidences"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: SURVEY_FEEDBACK_GET_ALL_ANSWERS
    Request_path: "{{ content_prefix }}/v1/surveys/getAllResponsesOfQuestion"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/surveys/getAllResponsesOfQuestion"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
      
  - name: DEEPLINK_VERIFY_OBSERVATION_LINK
    Request_path: "{{ content_prefix }}/v1/solutions/verifyLink"
    upstream_url: "{{ knowledge_mw_service_url }}/v1/solutions/verifyLink"
    strip_request_path: true
    plugins:
    - name: jwt
    - name: cors
    - "{{ statsd_pulgin }}"
    - name: acl
      config.whitelist:
        - 'contentAdmin'
    - name: rate-limiting
      config.policy: local
      config.hour: "{{ medium_rate_limit_per_hour }}"
      config.limit_by: credential
    - name: request-size-limiting
      config.allowed_payload_size: "{{ medium_request_size_limit }}"
 
      
            
      
      
      
      
      
      
            
      
            
      
            
      
      
      
            
      
                                          
                      
