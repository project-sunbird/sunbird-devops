---
- name: reload systemd configuration
  become: yes
  systemd:
    daemon_reload: true

# handlers file for ansible-cassandra
- name: "restart cassandra"
  service:
    name: "cassandra"
    state: "restarted"
