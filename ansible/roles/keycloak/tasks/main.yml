- include: provision.yml
  tags:
    - provision

- include: deploy.yml
  tags:
    - deploy

- include: auth-deploy.yml
  tags:
    - auth-deploy

- include: auth-deploy-config.yml
  tags:
    - auth-deploy-config

- include: keycloak_bootstrap.yml
  tags:
    - keycloak-bootstrap
