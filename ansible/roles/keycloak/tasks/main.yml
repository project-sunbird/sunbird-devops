- include: provision.yml
  tags:
    - provision

# Build keycloak sunbird auth package
- include: build-auth.yml
  tags:
    - build-auth

# Deploy keycloak sunbird auth package
- include: deploy-auth.yml
  tags:
    - deploy-auth

# Build keycloak sunbird auth configuration
- include: build-config.yml
  tags:
    - build-config

# Deploy keycloak sunbird auth configuration
- include: deploy-config.yml
  tags:
    - deploy-config

- include: keycloak_bootstrap.yml
  tags:
    - keycloak-bootstrap

# This is kept for backward compatiblity
- include: deploy.yml
  tags:
    - deploy
