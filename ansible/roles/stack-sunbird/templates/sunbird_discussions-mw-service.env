nodebb_service_url={{ discussionsmw_nodebb_service_url | default('http://nodebb-service:4567') }}

# Steps to generating the Nodebb auth token
# 1. Login to Nodebb (https://<domain-name>/discussions/login) as a Admin.
# 2. go to admin panel.
# 3. click on Plugins => Write API
# 4. enter UID as 1 and click create token (Auth token).
authorization_token={{ discussionsmw_nodebb_authorization_token }}
