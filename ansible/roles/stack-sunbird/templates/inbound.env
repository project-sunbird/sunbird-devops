spring.kafka.producer.value-serializer=org.springframework.kafka.support.serializer.JsonSerializer
spring.kafka.consumer.value-deserializer=org.springframework.kafka.support.serializer.JsonDeserializer
spring.kafka.consumer.auto-offset-reset=earliest

# Kafka
spring.kafka.bootstrap-servers=${BOOTSTRAP_SERVERS}
#spring.kafka.properties.security.protocol=SASL_SSL
#spring.kafka.properties.sasl.jaas.config=${JASS_CONFIG}
#spring.kafka.properties.sasl.mechanism=PLAIN
#spring.kafka.properties.ssl.endpoint.identification.algorithm=https

# TODO: Remove this moving forward and inject DAO.
spring.data.cassandra.contactpoints=127.0.0.1
spring.data.cassandra.port=9042
spring.data.cassandra.keyspace-name=testKeyspace
logging.level.root=INFO

# Confluent Cloud Schema Registry
schema.registry.url=${REGISTRY_URL}
basic.auth.credentials.source=USER_INFO
schema.registry.basic.auth.user.info=${AUTH_USER_INFO}

campaign=campaign
inboundUnprocessed=inbound-unprocessed
inboundProcessed=inbound-processed
gupshup-opted-out=gs-opted-out
inbound-error=inbound-error
server.port=8085

provider.gupshup.whatsapp.appname=Ekstep
campaign.url = ${CAMPAIGN_URL}



