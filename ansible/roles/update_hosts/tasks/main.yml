- name: Copy the generate_host.j2
  template: src=generate_host.j2 dest="{{host_path}}"/generate_host.sh mode=775

- name: Run the generate_host.sh script to create host file
  shell: "{{host_path}}"/generate_host.sh  >"{{host_path}}"/hosts 2>&1