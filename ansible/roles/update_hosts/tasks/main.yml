- name: Copy the generate_host.j2
  template: src=generate_host.j2 dest="../deploy/{{host_path}}/generate_host.sh" mode=775

- name: Run the generate_host.sh script to create host file
  command: ./generate_host.sh  > hosts 2>&1
  args:
      chdir: "../deploy/{{host_path}}"