---
hub_org: sunbird

registry_replicas: 1
registry_reservation_memory: 256M
registry_limit_memory: 512M

registry_url: "{{proto}}://{{proxy_server_name}}/registry"

registry_config: 
                - audit_frame.json    
                - frame.json
                - schema-configuration.jsonld
                - validations_create.shex
                - validations_update.shex