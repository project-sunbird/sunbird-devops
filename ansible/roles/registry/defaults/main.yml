---
hub_org: sunbird

xms: "-Xms1024m"
xmx: "-Xmx2048m"

registry_replicas: 1
registry_reservation_memory: 1500M
registry_limit_memory: 1500M

registry_url: "{{proto}}://{{proxy_server_name}}/registry"

registry_config: 
                - audit_frame.json    
                - frame.json
                - schema-configuration.jsonld
                - validations_create.shex
                - validations_update.shex